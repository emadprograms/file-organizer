---
status: investigating
trigger: |
  DATA_START
  investigate why is it null. is there something in the backend that caused this issue. this is not supposed to happen from what I understand.
  DATA_END
---
# Debug Session: manifest is null in state.json

## Symptoms
- **Expected behavior**: The `manifest` field inside `_state.json` files should not be `null`. It should contain the expected manifest data structure.
- **Actual behavior**: For some houses (like those in Safra C), the `manifest` field is `null`.
- **Error messages**: N/A
- **Timeline**: Occurs in `_state.json` files generated by the backend pipeline.
- **Reproduction**: Inspect the backend code that generates or updates `_state.json` (likely the ingest or data processing pipeline) to see why `manifest` gets set to `null` under certain conditions.

## Current Focus
- hypothesis: 
- next_action: gather initial evidence
