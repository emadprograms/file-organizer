# Phase 113: End-to-End Test Suite Verification & Milestone Audit - Plan

**Phase:** 113
**Goal:** Implement API-level backend integration tests and a dedicated end-to-end frontend test suite for non-residing applicants; verify complete 100% test coverage; perform milestone audit and finalize Milestone v15.0.
**Requirements:** VER-01, VER-02

---

## Tasks

### Task 1: Backend API Integration Tests (VER-01)
- In `tests/HousingApplication.Tests/ApiEndpointTests.cs`:
  - Add integration tests via `HttpClient`:
    1. `GetHouseProfile_WithResidentAndApplicant_ReturnsSegregatedProfile`: tests `GET /api/areas/{areaId}/houses/{houseId}/profile` returning both resident (`is_resident = 1`) and applicant (`is_resident = 0`) with notes.
    2. `BulkUpdateTenants_WithApplicant_SavesAndDoesNotReallocateDocsToApplicant`: tests `POST /api/areas/{areaId}/houses/{houseId}/tenants` saving applicant and reallocating documents only to resident.
    3. `GetTimeline_ReturnsApplicantStatus`: tests `GET /api/areas/{areaId}/houses/{houseId}/timeline` returning `is_resident = 0` for documents belonging to applicants.
    4. `Search_ReturnsApplicantBadgeAndNotCurrent`: tests `GET /api/search?q=...` returning applicant tenant search results with `is_resident = 0`, `is_current = false`, `duration_category = null`.

### Task 2: Dedicated Frontend End-to-End Workflow Test Suite (VER-02)
- Create `tests/frontend/components/applicant_workflow.test.js`:
  - Test 1: Segregated House Profile rendering and applicant card styling (`📋 متقدم (لم يسكن)`, order date, notes, doc counts).
  - Test 2: Applicant card click navigates to applicant's tenant folder route.
  - Test 3: House settings modal applicant row interactions (type toggle, disabled fields, payload generation).
  - Test 4: Ingest station and batch move/copy modals formatting applicant options with clipboard icon and non-residing notice.
  - Test 5: Timeline view document card rendering purple applicant badge for applicant documents.

### Task 3: Complete Test Suite Verification
- Run `export PATH="$HOME/.dotnet:$PATH" && dotnet test HousingApplication.sln` (all backend tests pass).
- Run `npm run test:frontend` (all frontend tests pass).

### Task 4: Milestone Audit & Completion
- Verify all 17 requirements against implementation.
- Generate `.planning/v15.0-MILESTONE-AUDIT.md`.
- Update `REQUIREMENTS.md`, `ROADMAP.md`, and `STATE.md`.

---

## Verification Criteria
1. All backend tests pass 100%.
2. All frontend Vitest tests pass 100%.
3. Milestone audit confirms 17/17 requirements satisfied.
