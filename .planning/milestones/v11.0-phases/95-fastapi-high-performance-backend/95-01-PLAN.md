# Plan 95-01: FastAPI High-Performance Backend

## Tasks
1. TDD Phase 1 - Write API v11 Tests:
   - Create `tests/test_api_v11.py` testing:
     - `/api/tree` against populated SQLite DB: verifies accurate areas, houses, tenure duration, counts, category breakdowns.
     - `/api/areas/{area}/houses/{house}/timeline` against DB: verifies sorted documents, titles, dates, tenants.
     - `/api/areas/{area}/houses/{house}/categories` against DB: verifies grouped documents by category.
     - `/api/search` against DB: verifies instant matching on house ID, tenant name, and Arabic titles.
     - `/api/areas/{area}/houses/{house}/pdf/{vault_id}`: verifies PDF serving from `vault/`.
     - Performance assertion: `/api/tree` benchmark executes in < 50ms (typically < 10ms).
2. Implement DB-backed API Endpoints:
   - Update `src/core/config.py` to support `db_path` (defaulting to e.g. `areas_root_path/organizer.db` or local).
   - Update `src/api/server.py` to initialize DB connection / repo on startup.
   - Update `src/api/routes.py` to implement DB query paths for tree, timeline, categories, search, and pdf.
3. Verify test suites:
   - Run `./.venv/bin/pytest tests/test_api_v11.py tests/test_api_grid_overview.py`.
   - Verify 100% pass and no regressions.
4. Write `.planning/phases/95-fastapi-high-performance-backend/95-01-SUMMARY.md`.
