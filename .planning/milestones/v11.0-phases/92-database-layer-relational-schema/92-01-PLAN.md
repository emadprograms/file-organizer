# Plan 92-01: Database Layer & Relational Schema

## Tasks
1. TDD Phase 1 - Write Schema & Connection Tests:
   - Create `tests/db/test_schema.py` verifying table creation, foreign key cascading, unique constraints, and WAL pragmas.
   - Verify tests fail initially (Red).
2. Implement Core Database Module:
   - Create `src/db/schema.py`: SQL DDL scripts, initialization, index creation.
   - Create `src/db/connection.py`: Database connection factory with WAL mode and foreign key pragmas enabled.
   - Create `src/db/models.py`: Clean models for Area, House, Tenant, Batch, Page, Document.
3. TDD Phase 2 - Write Repository Tests:
   - Create `tests/db/test_repository.py` testing CRUD operations for Areas, Houses, Tenants, Batches, Pages, and Documents.
4. Implement Repository:
   - Create `src/db/repository.py` with typed, transactional CRUD methods.
5. Run full test suite and verify 100% pass.
