---
phase: 09-rate-limiting-router-safety-net
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/exceptions.py
  - src/llm/llm.py
  - src/processing/routing/router.py
  - tests/test_llm_resilience.py
autonomous: true
requirements:
  - RES-01
  - RES-02
  - RES-03
must_haves:
  truths:
    - 429 errors trigger exactly 65s sleep and 3 retries before halting.
    - 500/503 errors trigger 15s sleep and immediate provider rotation.
    - 401/403 errors cause immediate pipeline halt.
    - Secondary providers alternate between Groq and OpenRouter across requests.
  artifacts:
    - src/core/exceptions.py (PipelineHaltError added)
    - src/llm/llm.py (Updated _route_llm_call, dead code removed)
    - tests/test_llm_resilience.py (TDD tests with mocked sleep)
  key_links:
    - LLMClient._route_llm_call -> time.sleep (Correct durations)
    - LLMClient._route_llm_call -> self._fallback_toggle (Thread-safe alternation)
---

<objective>
Implement a deterministic LLM resilience loop that replaces exponential backoff with strict, requirement-driven wait times and provider rotation, while ensuring a "Correctness First" failure model.

Purpose: Prevent silent failures and mis-routing by halting the pipeline on unrecoverable LLM errors.
Output: A thread-safe, deterministic LLM orchestration layer.
</objective>

<execution_context>
@/Users/emadarshadalam/Documents/GitHub/File-Categorizer/.gemini/gsd-core/workflows/execute-plan.md
@/Users/emadarshadalam/Documents/GitHub/File-Categorizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/llm/llm.py
@src/core/exceptions.py
@src/processing/routing/router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish Pipeline Halt Exception Hierarchy</name>
  <files>src/core/exceptions.py, src/llm/llm.py, src/processing/routing/router.py</files>
  <action>
    1. In `src/core/exceptions.py`, create `PipelineHaltError` inheriting from `FileOrganizerError`.
    2. In `src/llm/llm.py`, update `LLMFailureError` to inherit from `PipelineHaltError`.
    3. In `src/processing/routing/router.py`, update `RoutingValidationError` to inherit from `PipelineHaltError`.
    This ensures that any "Hard Stop" error can be caught by a single base class in the pipeline orchestrator (preparing for Phase 10).
  </action>
  <verify>
    <automated>python3 -c "from src.core.exceptions import PipelineHaltError; from src.llm.llm import LLMFailureError; from src.processing.routing.router import RoutingValidationError; assert issubclass(LLMFailureError, PipelineHaltError); assert issubclass(RoutingValidationError, PipelineHaltError)"</automated>
  </verify>
  <done>Exception hierarchy established: FileOrganizerError -> PipelineHaltError -> {LLMFailureError, RoutingValidationError}</done>
</task>

<task type="tdd">
  <name>Task 2: TDD Resilience Suite with Mocked Sleep</name>
  <files>tests/test_llm_resilience.py</files>
  <behavior>
    - Test Case 1 (429): Mock provider to return 429. Verify `time.sleep` called with 65 exactly 3 times. Verify `LLMFailureError` raised.
    - Test Case 2 (500): Mock provider to return 500. Verify `time.sleep` called with 15. Verify provider sequence rotates (Gemini -> S1 -> Gemini -> S2). Verify `LLMFailureError` after 3 failures.
    - Test Case 3 (401): Mock provider to return 401. Verify immediate `LLMFailureError` with no sleep.
    - Test Case 4 (Alternation): Verify that across two sequential `_route_llm_call` invocations, the secondary provider used first alternates (S1 then S2).
  </behavior>
  <action>
    Create `tests/test_llm_resilience.py`. Use `unittest.mock.patch('time.sleep')` to assert call arguments and prevent real waiting. Mock `LLMProvider` implementations to simulate specific HTTP status codes.
  </action>
  <verify>
    <automated>pytest tests/test_llm_resilience.py</automated>
  </verify>
  <done>All resilience test cases fail (RED) as expected with current implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Deterministic Resilience Loop</name>
  <files>src/llm/llm.py</files>
  <behavior>
    - Replace `tenacity` retry logic in `_route_llm_call` with a manual `for attempt in range(max_attempts)` loop.
    - If 429: `time.sleep(65)`, retry same provider.
    - If 500/503: `time.sleep(15)`, move to next provider in sequence.
    - If 401/403/400: Raise `LLMFailureError` immediately.
    - Reuse `self._fallback_toggle` and `self._fallback_toggle_lock` for thread-safe provider alternation.
    - Remove `self.global_consecutive_500_errors` and all related tracking logic.
    - Ensure the loop raises `LLMFailureError` upon exhaustion of all attempts across the sequence.
  </behavior>
  <action>
    Refactor `_route_llm_call` in `src/llm/llm.py`. 
    1. Remove `@tenacity.retry` and the inner `_call_provider` function.
    2. Implement the sequence: [Gemini, S1, Gemini, S2].
    3. Implement the a manual retry loop with strict `time.sleep` values.
    4. Remove the legacy `global_consecutive_500_errors` counter from `__init__` and throughout the method.
    5. Use the existing `_fallback_toggle` logic to determine S1 and S2 for the current request.
  </action>
  <verify>
    <automated>pytest tests/test_llm_resilience.py</automated>
  </verify>
  <done>Deterministic resilience loop implemented and verified by TDD suite; dead code removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LLM API -> LLMClient | Untrusted HTTP responses (429, 500, 401) |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Denial of Service | LLMClient | medium | mitigate | Strict 3-retry limit prevents infinite loops on API failure. |
| T-09-02 | Tampering | Provider Rotation | low | mitigate | Use of `_fallback_toggle_lock` ensures deterministic alternation in multi-threaded environments. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced in this plan. |
</threat_model>

<verification>
Run the TDD suite to confirm all resilience requirements (429, 500, 401) and rotation logic are exactly as specified.
</verification>

<success_criteria>
- 429s wait 65s (verified via mock).
- 500s wait 15s and rotate (verified via mock).
- 401s halt immediately.
- Rotation alternates providers per request.
- Legacy 500 counter removed.
- `LLMFailureError` inherits from `PipelineHaltError`.
</success_criteria>

<output>
Create `.planning/phases/09-rate-limiting-router-safety-net/09-01-SUMMARY.md` when done
</output>
