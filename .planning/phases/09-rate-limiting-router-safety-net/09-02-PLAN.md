---
phase: 09-rate-limiting-router-safety-net
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/processing/routing/router.py
  - tests/test_routing_safety.py
autonomous: true
requirements:
  - RES-02
must_haves:
  truths:
    - No documents are routed to '13_others' as a fallback for LLM failures.
    - No documents are routed to '13_others' when category mappings are missing.
    - No lockout mechanism exists for consecutive routing failures.
  artifacts:
    - src/processing/routing/router.py (Fallbacks and lockout logic removed)
    - tests/test_routing_safety.py (Verification tests for hard halts)
  key_links:
    - route_document -> RoutingValidationError (Halt on any mapping failure)
---

<objective>
Eliminate all "graceful" fallbacks in the document router to ensure that any routing ambiguity or failure results in a pipeline halt rather than a silent mis-classification into '13_others'.

Purpose: Enforce the "Correctness First" mandate by preventing silent failures.
Output: A strictly validated routing function that either returns a correct folder or halts the system.
</objective>

<execution_context>
@/Users/emadarshadalam/Documents/GitHub/File-Categorizer/.gemini/gsd-core/workflows/execute-plan.md
@/Users/emadarshadalam/Documents/GitHub/File-Categorizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/processing/routing/router.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Safety Net Test Suite</name>
  <files>tests/test_routing_safety.py</files>
  <behavior>
    - Test Case 1 (Unmapped Category): Provide a category with no mapping in `CATEGORY_TO_FOLDERS`. Verify `RoutingValidationError` is raised instead of returning '13_others'.
    - Test Case 2 (LLM Exhaustion): Mock `LLMClient` to raise `LLMFailureError`. Verify that the error propagates and halts the router.
    - Test Case 3 (Validation Failure): Mock `LLMClient` to return an invalid folder. Verify that after 3 attempts, `RoutingValidationError` is raised.
    - Test Case 4 (Lockout Removal): Verify that multiple sequential failures do not trigger a "lockout" or skip routing.
  </behavior>
  <action>
    Create `tests/test_routing_safety.py`. Use `pytest.raises` to assert that `RoutingValidationError` or `LLMFailureError` is thrown in all previous "fallback" scenarios.
  </action>
  <verify>
    <automated>pytest tests/test_routing_safety.py</automated>
  </verify>
  <done>All safety net tests fail (RED) because existing code still uses fallbacks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surgical Removal of Router Fallbacks</name>
  <files>src/processing/routing/router.py</files>
  <behavior>
    - Remove the global `consecutive_routing_failures` variable and all logic that checks or increments it.
    - In `route_document`, replace the fallback `return "13_others", False` inside the `if not allowed_folders:` block with `raise RoutingValidationError(f"Category '{category}' has no mapping. Routing cannot proceed.")`.
    - Remove the final fallback `return "13_others", False` at the end of the `route_document` function, ensuring the loop's final `raise` is the only exit point on failure.
  </behavior>
  <action>
    1. Delete `consecutive_routing_failures = 0` (line 12) and the lockout check (lines 42-45).
    2. Edit the `if not allowed_folders:` block (lines 57-59) to raise `RoutingValidationError`.
    3. Delete the final `return "13_others", False` (line 153).
    4. Remove the `consecutive_routing_failures = 0` reset (line 107).
  </action>
  <verify>
    <automated>pytest tests/test_routing_safety.py</automated>
  </verify>
  <done>All fallbacks and lockout logic removed from the router; TDD suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Router -> Pipeline | Exception propagation to trigger hard stop |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Information Disclosure | Router | low | accept | Errors contain category names which are not sensitive. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced in this plan. |
</threat_model>

<verification>
Execute the safety net test suite to ensure that no path through `route_document` can return '13_others' unless the LLM explicitly selected it as the best fit from the allowed list.
</verification>

<success_criteria>
- `consecutive_routing_failures` is completely removed from the codebase.
- Missing category mappings raise `RoutingValidationError`.
- LLM failure/exhaustion raises `LLMFailureError` or `RoutingValidationError`.
- No implicit fallback to '13_others' exists.
</success_criteria>

<output>
Create `.planning/phases/09-rate-limiting-router-safety-net/09-02-SUMMARY.md` when done
</output>
