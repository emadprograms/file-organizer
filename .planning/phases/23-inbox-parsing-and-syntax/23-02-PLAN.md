---
wave: 2
depends_on: ["23-01-PLAN.md"]
files_modified:
  - src/inbox/resolver.py
  - src/main.py
  - tests/test_resolver.py
autonomous: true
---

# Phase 23 - Plan 02: Inbox Data Inference and Resolution

## Requirements
- FSUI-02
- FSUI-03

## Goal
Build the resolution logic to infer missing 'U' data using majority-vote from `_report.json`, resolve area directory conflicts, and integrate these capabilities into the `append` mode CLI hook.

## must_haves
- U resolution uses majority vote on _report.json pages
- House area conflicts raise specific errors for renaming
- Tenant hint resolves via canonicalize_with_llm
- Append mode in main.py catches errors and renames file with specific suffixes

## Tasks

<task>
  <read_first>
    - .planning/phases/23-inbox-parsing-and-syntax/23-CONTEXT.md
    - .planning/phases/23-inbox-parsing-and-syntax/23-RESEARCH.md
    - src/categorization/categorization.py
    - src/inbox/resolver.py
  </read_first>
  <action>
    Create src/inbox/resolver.py. Implement infer_missing_data function taking a PDF path and a ParsedCommand.
    If the parsed house or date is 'U', call process_unclassified_pdf from categorization.py to generate _report.json.
    Parse the resulting JSON and implement majority-vote logic across all page entries for expected_house_number and date.
    Return a dictionary with the inferred house and date values.
  </action>
  <acceptance_criteria>
    - grep -q "def infer_missing_data" src/inbox/resolver.py
    - Source assertion confirms function uses collections.Counter or similar logic for the majority vote.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - .planning/phases/23-inbox-parsing-and-syntax/23-CONTEXT.md
    - src/core/config.py
    - src/tenant_config/yaml_loader.py
    - src/grouping/name_matcher.py
    - src/inbox/resolver.py
    - src/routing/config.py
  </read_first>
  <action>
    In src/inbox/resolver.py, add a ConflictError exception class.
    Implement resolve_area function: Scan directories in areas_root looking for the house ID. If found in multiple areas, raise ConflictError. If not found, raise ValueError. Return the matched Area ID.
    Implement resolve_tenant function: If tenant_hint is not 'U', load tenants via yaml_loader.py and call canonicalize_with_llm. Return the canonical match or 'U' if no match.
    Implement resolve_group_mode function: Take the `group` value and determine routing instruction. If '1'-'13', map to corresponding FOLDER_PREFIXES from src/routing/config.py, and return instruction to skip grouping and routing. If 'G', return instruction to skip grouping but run routing. If 'U', return instruction to run both grouping and routing.
  </action>
  <acceptance_criteria>
    - grep -q "class ConflictError" src/inbox/resolver.py
    - grep -q "def resolve_area" src/inbox/resolver.py
    - grep -q "def resolve_tenant" src/inbox/resolver.py
    - grep -q "def resolve_group_mode" src/inbox/resolver.py
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/main.py
    - src/inbox/parser.py
    - src/inbox/resolver.py
  </read_first>
  <action>
    Update src/main.py in the run_append_mode function.
    Inside the listener loop for each PDF in the inbox_path:
    Invoke parse_filename_syntax. Catch ValueError and use os.rename to append _Error_Invalid_Format to the filename before .pdf. Continue to next file.
    Invoke infer_missing_data and resolve_area. Catch ConflictError and use os.rename to append " - please choose area" before .pdf. Continue to next file.
    (Do not execute the rename-to-proposed or finalization logic, which belongs to Phase 24).
  </action>
  <acceptance_criteria>
    - grep -q "parse_filename_syntax" src/main.py
    - Behavior assertion: Running append mode on a file missing 5 tokens renames it to *_Error_Invalid_Format.pdf
    - Behavior assertion: Area conflict throws ConflictError and renames file to * - please choose area.pdf
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - tests/test_resolver.py
  </read_first>
  <action>
    Create tests/test_resolver.py. Add unit tests for infer_missing_data (mocking categorization output), resolve_area (mocking os.walk/Path.iterdir), and resolve_tenant (mocking LLM).
  </action>
  <acceptance_criteria>
    - pytest tests/test_resolver.py passes successfully
  </acceptance_criteria>
</task>

## Verification
- Verify that `append` mode successfully processes a file with a 'U' flag by generating `_report.json` and computing the mode.
- Verify that invalid filenames trigger the appropriate rename suffix without crashing the listener loop.

## Artifacts this phase produces
- `src/inbox/resolver.py`: New file containing `infer_missing_data`, `resolve_area`, `resolve_tenant`, and `ConflictError`
- `src/main.py`: Modified `run_append_mode` behavior
- `tests/test_resolver.py`: New unit tests for resolver logic
