---
phase: 07.3-improve-multi-page-correspondence-processing-via-arabic-foot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline.py
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - Pass 1 bypasses the LLM via Vision OCR if an Arabic footer is detected on the bottom 10% of a page
    - Pass 2 correctly groups pages by looking ahead and tolerating single missing footers
  artifacts: []
  key_links: []
---

<objective>
Drastically speed up processing by detecting multi-page correspondences and skipping page-by-page LLM extraction for the subsequent pages of the same document.

Purpose: Avoid heavy and expensive LLM calls on continuation pages while maintaining accurate grouping logic.
Output: Modified Pass 1 extraction logic and Pass 2 grouping logic in `src/pipeline.py`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.3-improve-multi-page-correspondence-processing-via-arabic-foot/07.3-PATTERNS.md
@.planning/phases/07.3-improve-multi-page-correspondence-processing-via-arabic-foot/07.3-RESEARCH.md
</context>

<tasks>

<task type="auto" autonomous="true">
  <name>Task 1: Add Vision OCR integration to process_single_page in src/pipeline.py</name>
  <files>src/pipeline.py</files>
  <action>Inside `process_single_page`, immediately after the blank page size check and before the `client.classify_page(i_bytes)` call, implement a Mac Vision OCR check. Conditionally import `pyobjc-framework-Vision` and `pyobjc-framework-Quartz` (or handle dynamically). Convert `i_bytes` for the Vision framework. Execute `VNRecognizeTextRequest` specifically targeted at the bottom 10% (`CGRectMake(0.0, 0.0, 1.0, 0.1)`) with `setRecognitionLanguages_(["ar"])`. Use regex or flexible string matching to look for pagination footers like "الصفحة 2" or "1 من 10". If found, bypass the LLM completely: cache and return a dummy `PageClassification` with `is_continuation=True`, `category=Category.OTHER_LETTERS`, and `residents=["UNKNOWN"]`.</action>
  <verify>Syntax check `src/pipeline.py`.</verify>
  <done>If a footer is detected, the LLM is correctly skipped, and a dummy `PageClassification` with `is_continuation=True` is cached and returned.</done>
</task>

<task type="auto" autonomous="true">
  <name>Task 2: Refactor _group_pages_into_documents for Lookahead in src/pipeline.py</name>
  <files>src/pipeline.py</files>
  <action>Update the `_group_pages_into_documents` function to bypass strict categorization and tenant matching when pages have `is_continuation=True` set. Modify the `raw_pages` iteration to implement a lookahead window (e.g., using `enumerate` on the list). If `page[i]` lacks `is_continuation=True` but `page[i+1]` has it, safely merge `page[i]` into the current document group, tolerating the missing or misprinted footer. Ensure a sequence is only broken (initializing a new document group) when multiple consecutive pages fail the lookahead/continuation check.</action>
  <verify>Syntax check `src/pipeline.py`.</verify>
  <done>Grouping logic handles dummy categories securely and merges missing-footer intermediate pages into their document group by looking ahead.</done>
</task>

<task type="checkpoint" autonomous="false">
  <name>Task 3: Integration testing and validation</name>
  <files>src/pipeline.py</files>
  <action>Run a test batch containing a multi-page document with an Arabic footer. Verify logs show the LLM was skipped for the continuation pages. Confirm the output folder structure groups the entire sequence correctly despite the LLM skips, including any intentionally missing single intermediate footer.</action>
  <verify>Manual visual check of output logs and directory folders.</verify>
  <done>Multi-page sequences are correctly processed, grouped together, and heavily optimized by bypassing the LLM via OCR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File I/O | Reading from local disk into memory for Vision framework processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.3-01 | Tampering | Vision OCR processing | accept | Vision framework runs locally on inherently trusted input data; no remote payload execution risk. |
</threat_model>

<verification>
Check that Pass 1 continues safely skipping continuation pages and that Pass 2 correctly handles the lookahead for grouping.
</verification>

<success_criteria>
- Mac Vision framework successfully extracts Arabic text from the bottom 10% of pages.
- Pass 1 execution time drops drastically for multi-page correspondences because the LLM is skipped.
- Pass 2 accurately groups all correspondence pages into the same directory category.
</success_criteria>

<output>
Create `.planning/phases/07.3-improve-multi-page-correspondence-processing-via-arabic-foot/07.3-01-SUMMARY.md` when done
</output>
