# Phase 19.1: Fix YAML Integration Architecture - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-16
**Phase:** 19.1-fix-yaml-integration-architecture
**Areas discussed:** YAML file naming convention, YAML schema structure, Fallback orchestration

---

## YAML file naming convention

| Option | Description | Selected |
|--------|-------------|----------|
| 1 | Use `{house_id}_tenants.yaml` to support multiple houses in the same source directory. | ✓ |
| 2 | Use a single `tenants.yaml` for simplicity. | |

**User's choice:** keep the current implementation. that should not change. use {house_id}_tenants.yaml, if that is the current implementation.
**Notes:** Decided to retain backward compatibility with the existing house-based naming convention.

---

## YAML schema structure

| Option | Description | Selected |
|--------|-------------|----------|
| 1 | Update `yaml_loader.py` to parse the complex schema (list of dicts with name, start_date, end_date) that `phase.py` is currently expecting. | ✓ |
| 2 | Change the schema to be a simple list of names `{"tenants": ["name1", "name2"]}` everywhere, dropping explicit timelines from YAML. | |

**User's choice:** Option 1
**Notes:** None

---

## Fallback orchestration

| Option | Description | Selected |
|--------|-------------|----------|
| 1 | `yaml_loader.py` should return `None` or an empty list when the file is missing, signaling `pipeline.py` to gracefully fallback to anchor logic. | ✓ |
| 2 | `yaml_loader.py` should raise a specific error (e.g. `FileNotFoundError`) when missing, and `pipeline.py` should catch it to trigger the fallback. | |

**User's choice:** Option 1
**Notes:** None
