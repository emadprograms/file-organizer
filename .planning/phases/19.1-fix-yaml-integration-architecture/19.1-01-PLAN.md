---
wave: 1
depends_on: []
files_modified:
  - src/tenant_config/yaml_loader.py
  - src/timeline/phase.py
  - src/pipeline/pipeline.py
  - src/main.py
autonomous: true
---

# Phase 19.1 Plan: Fix YAML Integration Architecture

## Tasks

```xml
<task>
<read_first>
- src/tenant_config/yaml_loader.py
</read_first>
<action>
Update `load_tenant_config` to accept `(target_dir: Path, house_id: str) -> list[dict] | None`.
Update the file path resolution to use `f"{house_id}_tenants.yaml"`.
If the file does not exist, log a warning `"No {yaml_path.name} found in {target_dir}. Falling back to anchor logic."` and return `None` (D-03).
Load the YAML file using `yaml.safe_load(f) or []`.
Ensure the loaded data is a `list`. If not, raise `ConfigurationError(f"YAML must be a list of dictionaries in {yaml_path}")`.
Iterate through the list and explicitly validate the complex schema: ensure each item is a `dict` containing `name`, `start_date`, and `end_date` keys. If validation fails, raise `ConfigurationError`.
Return the loaded data (D-02).
</action>
<acceptance_criteria>
- `yaml_loader.py contains def load_tenant_config(target_dir: Path, house_id: str) -> list[dict] | None:`
- `yaml_loader.py` returns `None` if the file does not exist.
- `yaml_loader.py` raises `ConfigurationError` if the loaded data is not a list.
- `yaml_loader.py` explicitly validates that each item in the list is a dict and has `name`, `start_date`, and `end_date` keys.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/timeline/phase.py
</read_first>
<action>
Modify the signature of `process_cleaning_phase` to be `def process_cleaning_phase(json_path: Path, llm_client: LLMClient, yaml_data: list[dict] | None) -> tuple[list[PageData], list[dict] | None]:`.
Remove the inline YAML file checking and loading logic (specifically the lines referencing `target_dir / f"{house_id}_tenants.yaml"` and `yaml.safe_load`).
Instead, check `if yaml_data is not None:`. If it is not `None`, populate `allowed_tenants` and `yaml_timelines` using the provided `yaml_data`. If `yaml_data` is `None`, log that it's using the anchor method.
</action>
<acceptance_criteria>
- `phase.py contains def process_cleaning_phase(json_path: Path, llm_client: LLMClient, yaml_data: list[dict] | None)`
- `phase.py` does not contain any references to `f"{house_id}_tenants.yaml"` or `yaml.safe_load`.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/pipeline/pipeline.py
</read_first>
<action>
Add `from pathlib import Path` to the top-level imports.
Add a new method `_clean_documents(self, json_path: Path, target_dir: Path, house_id: str) -> tuple[list, list[dict] | None]:` to the `Pipeline` class.
Inside `_clean_documents`:
1. Import `load_tenant_config` from `src.tenant_config.yaml_loader`.
2. Import `process_cleaning_phase` from `src.timeline.phase`.
3. Call `load_tenant_config(target_dir, house_id)` to retrieve `yaml_data`.
4. Call `process_cleaning_phase(json_path, self.client, yaml_data)` and return its result.
</action>
<acceptance_criteria>
- `pipeline.py contains def _clean_documents(self, json_path: Path, target_dir: Path, house_id: str)`
- `pipeline.py` imports and calls `load_tenant_config` inside `_clean_documents`.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/main.py
</read_first>
<action>
In the `run_cleaning_pass` function, replace the direct call to `process_cleaning_phase` with the pipeline integration.
Import `Pipeline` from `src.pipeline.pipeline`.
Instantiate it: `pipeline = Pipeline(api_key=os.getenv("GEMINI_API_KEY"))`.
Set the pipeline's client: `pipeline.client = llm_client`.
Call `cleaned_pages, yaml_data = pipeline._clean_documents(json_path, target_dir, house_id)`.
</action>
<acceptance_criteria>
- `main.py` contains `pipeline = Pipeline(api_key=os.getenv("GEMINI_API_KEY"))` inside `run_cleaning_pass`.
- `main.py` contains `cleaned_pages, yaml_data = pipeline._clean_documents(json_path, target_dir, house_id)`.
</acceptance_criteria>
</task>
```

## Verification

- `pytest` passes any existing unit tests for configuration loading and the pipeline.
- Running `python src/main.py` with an existing `_categorized.pdf` and `_report.json` correctly uses the `{house_id}_tenants.yaml` if it exists, or falls back gracefully without crashing.

## Must Haves

must_haves:
  truths:
    - "D-01: yaml_loader.py accepts house_id to resolve {house_id}_tenants.yaml"
    - "D-02: yaml_loader.py parses and explicitly validates the complex schema (list of dicts with name, start_date, end_date)"
    - "D-03: yaml_loader.py correctly returns None when the YAML file is missing, signaling pipeline.py to gracefully fallback to anchor logic"
    - "Pipeline._clean_documents acts as the orchestrator for configuration loading and phase processing"
  prohibitions: []

## Artifacts this phase produces
- `Pipeline._clean_documents` (Method)
