---
phase: 19
wave: 1
depends_on: []
requirements_addressed: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]
files_modified:
  - tests/
  - tests/fixtures/golden_1273/
autonomous: true
---

# Phase 19: End-to-End Testing and Verification Plan

## Goal
Overhaul the test suite, enforce test naming, restructure the golden dataset, mock LLM at function level, and add complete E2E routing assertions.

## Tasks

<task>
<read_first>
- tests/uat_08_contracts.py
- tests/uat_08_precision_window.py
- tests/uat_08_utility_bills.py
- tests/verify_phase08_grouping.py
- tests/verify_dual_logging.py
- tests/verify_jsonl_trace.py
- tests/verify_trace_refactor.py
- tests/verify_uat_09.py
- tests/verify_uat_11_1.py
- tests/verify_uat_11_2.py
- tests/verify_uat_11_3.py
- tests/verify_uat_11_4.py
- tests/verify_uat_11_9.py
- tests/verify_uat_11_others_flow.py
- tests/test_phase12_finalization.py
- tests/test_phase7_features.py
- tests/test_uat_09_01.py
- tests/test_uat_09_02.py
- tests/test_uat_09_03.py
</read_first>
<action>
Rename test files to descriptive names starting with `test_` (e.g., `verify_uat_11_1.py` to `test_routing_conditional_llm.py`). Examine the contents of each file to understand what it tests and assign an appropriate name reflecting the test's purpose rather than the phase. Update any cross-file test imports that might break due to the rename.
</action>
<acceptance_criteria>
All specified files are renamed to descriptive names. Running `pytest tests/` executes these tests without issue.
</acceptance_criteria>
</task>

<task>
<read_first>
- tests/uat_08_e2e_continuity.py
- tests/uat_08_e2e_default.py
- tests/uat_08_e2e_others.py
- tests/test_phase7_uat.py
</read_first>
<action>
Rewrite these standalone scripts as proper `pytest` modules. Wrap their execution logic inside `def test_*()` functions. Replace real LLM calls within these tests with mocked responses (or integrate them with the new function-level mocking mechanism).
</action>
<acceptance_criteria>
The 4 files are converted to standard pytest modules. They run seamlessly via `pytest tests/` and no standalone execution blocks (`if __name__ == "__main__":`) are relied upon for testing.
</acceptance_criteria>
</task>

<task>
<read_first>
- tests/verify_phase_09.py
- tests/create_continuity_fixture.py
- tests/conftest.py
</read_first>
<action>
Delete `tests/verify_phase_09.py` as it is redundant. Move the fixture generation logic and data from `tests/create_continuity_fixture.py` into `tests/conftest.py` as a `@pytest.fixture` or into `tests/fixtures/` as a static JSON data file. Then delete `tests/create_continuity_fixture.py`.
</action>
<acceptance_criteria>
`tests/verify_phase_09.py` and `tests/create_continuity_fixture.py` no longer exist. The continuity fixture is available in `conftest.py` or as static data.
</acceptance_criteria>
</task>

<task>
<read_first>
- tests/fixtures/golden_1273/
- src/tenant_config/yaml_loader.py
</read_first>
<action>
Restructure the `tests/fixtures/golden_1273/` fixture. Create an `input/` folder and an `expected_output/` folder inside it. Move the source data (e.g., `1273_categorized.pdf`) into `input/1273/`. Create a `.source_files/` directory specifically *inside* `input/1273/` and place the YAML config (e.g., `1273_tenants.yaml`) within it. Define the exact expected final folder and file structure inside `expected_output/`.
</action>
<acceptance_criteria>
The golden fixture has `input/1273/.source_files/1273_tenants.yaml` and an `expected_output/` directory. Tests loading the YAML path correctly resolve it inside the target house directory.
</acceptance_criteria>
</task>

<task>
<read_first>
- tests/test_e2e.py
- tests/test_yaml_pipeline.py
- src/pipeline/pipeline.py
- src/grouping/name_matcher.py
</read_first>
<action>
Capture real intermediate JSON state files (`1273_cleaned.json`, `1273_grouped.json`, `1273_3_routed_and_finalized.json`) and real LLM responses for `golden_1273` by running the pipeline once. Save these files into the fixture. Update E2E and dry-run tests to use these intermediate state files and implement function-level mocking (`canonicalize_with_llm`, `route_document`, etc.) that returns these saved LLM responses.
</action>
<acceptance_criteria>
Intermediate state JSON files and LLM mock responses exist in the golden fixture. E2E dry-run tests do not make external LLM calls and successfully use the mocked function returns and pre-computed state files.
</acceptance_criteria>
</task>

<task>
<read_first>
- tests/test_e2e.py
- tests/test_pipeline_routing.py
</read_first>
<action>
Add E2E routing destination assertions. In the E2E tests, after the pipeline splits and routes the files, traverse the output directory and assert that each specific file was correctly routed to the correct destination folder as defined in `golden_1273/expected_output/`.
</action>
<acceptance_criteria>
E2E tests verify the exact final file paths against `golden_1273/expected_output/`. The test suite maintains its passing state (all tests pass).
</acceptance_criteria>
</task>

## Verification
- must_have: Run `pytest tests/` without external LLM calls, and all tests must pass.
- must_have: All files in `tests/` must follow the `test_*.py` naming convention and be proper pytest modules. (covers TEST-01, D-01)
- must_have: Dry runs correctly utilize mocked LLM responses and intermediate JSONs. (covers TEST-04, TEST-05, D-05, D-06)
- must_have: `golden_1273` fixture has `input` and `expected_output` folders, with `.source_files` directly inside `input/1273/`. (covers TEST-02, TEST-03, D-02, D-03, D-04)
- must_have: E2E tests have exact assertions on routing destinations matching `expected_output`. (covers TEST-06)

## Artifacts this phase produces
- `tests/test_*.py` (Renamed and converted test modules)
- `tests/fixtures/golden_1273/input/`
- `tests/fixtures/golden_1273/expected_output/`
- `tests/fixtures/golden_1273/input/1273/.source_files/`
- `tests/fixtures/golden_1273/*_mock_responses.json` (or similar)
