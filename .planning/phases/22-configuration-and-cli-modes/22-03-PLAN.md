# Phase 22 Gap Closure Plan

## Task 1: Enforce Test Naming Convention
**Prompt:**
The UAT report indicated a gap: "also name the tests properly. the naming scheme shoudl test_(folder_name)_(file_name)_(what_is_being_tested).py"

Execute the following steps to close the gap:

1. Rename the tests introduced/modified in Phase 22 to match the `test_(folder_name)_(file_name)_(what_is_being_tested).py` scheme using `git mv`.
   - `tests/test_core_config.py` -> `tests/test_core_config_parsing.py` (Tests `src/core/config.py`)
   - `tests/test_main_cli.py` -> `tests/test_root_main_cli.py` (Tests `src/main.py` CLI logic; since `main.py` is in the root of `src/`, use `root` for the folder name)
   - `tests/test_listener_lock.py` -> `tests/test_root_main_append_mode.py` (Tests `src/main.py` append mode logic)

2. Run `pytest tests/test_core_config_parsing.py tests/test_root_main_cli.py tests/test_root_main_append_mode.py` to ensure the renamed tests pass and discover correctly.
