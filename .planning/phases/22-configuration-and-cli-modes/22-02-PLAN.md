---
phase: "22"
plan: "02"
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - requirements.txt
  - src/main.py
  - tests/test_main_cli.py
  - tests/test_listener_lock.py
autonomous: true
requirements:
  - CONF-02
  - CONF-03
must_haves:
  truths:
    - User can run `create <path>` to process a PDF.
    - User can run `append` to start the listener, which gracefully enforces mutual exclusion.
  artifacts:
    - src/main.py
    - tests/test_main_cli.py
    - tests/test_listener_lock.py
  key_links:
    - CLI arguments router directs to standard pipeline for `create` and a stub listener for `append`.
---

<objective>
Refactor CLI using argparse subparsers and implement the mutually exclusive `append` listener mode stub.
Purpose: Allow multi-mode application usage, splitting the history-building logic (`create`) from the upcoming UI orchestration loop (`append`).
Output: CLI argument router, lockfile mutual exclusion implementation, and respective test coverage.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-configuration-and-cli-modes/22-CONTEXT.md
</context>

## Artifacts this phase produces

- `requirements.txt`: Updated to include `filelock`.
- `src/main.py`: Updated with subparsers and the lockfile logic.
- `tests/test_main_cli.py`: Updated tests for the new CLI.
- `tests/test_listener_lock.py`: Tests for the `.inbox.lock` mechanism.

<tasks>
<task type="auto">
  <name>Task 1: Add filelock dependency and implement append mode stub</name>
  <files>requirements.txt, src/main.py</files>
  <read_first>
    - .planning/phases/22-configuration-and-cli-modes/22-RESEARCH.md (Pattern 2: Process Exclusive Lock)
  </read_first>
  <action>
    Add `filelock` to `requirements.txt`.
    In `src/main.py`, implement `run_append_mode(config)` which attempts to acquire `.inbox.lock` in the `config.inbox_path` using `filelock.FileLock(timeout=0)` (per D-07).
    If the lock is acquired, print a stub message e.g., "Listener started..." (per D-08).
    If a `Timeout` is raised (lock already taken), gracefully log and `sys.exit(0)`.
  </action>
  <verify>
    <automated>grep -q "filelock" requirements.txt && pytest tests/test_listener_lock.py -x || echo "MISSING — Wave 0 must create tests/test_listener_lock.py first"</automated>
  </verify>
  <acceptance_criteria>
    - `filelock` is correctly listed in `requirements.txt`.
    - `run_append_mode` blocks parallel executions by respecting the lockfile.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement argparse subparsers and boundary checks</name>
  <files>src/main.py</files>
  <read_first>
    - .planning/phases/22-configuration-and-cli-modes/22-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    Refactor `get_parser()` in `src/main.py` to use `add_subparsers(dest="command")`.
    Create `create` subparser taking `<path>`. Ensure it enforces the strict path constraint: the provided path MUST be resolved and be relative to (`is_relative_to`) `config.areas_root_path`. Throw an error if it's outside (per D-05).
    For `create` mode, route to the existing document pipeline logic (per D-06).
    Create `append` subparser. Route it to the `run_append_mode` stub.
  </action>
  <verify>
    <automated>pytest tests/test_main_cli.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The CLI supports `python main.py create <path>` and `python main.py append`.
    - `create` mode throws a deterministic error when passed a path outside the configured `areas_root_path`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test CLI routing and lockfile mutual exclusion</name>
  <files>tests/test_main_cli.py, tests/test_listener_lock.py</files>
  <read_first>
    - src/main.py
  </read_first>
  <action>
    Update `tests/test_main_cli.py` to test the new subparser routing, ensuring `create` fails appropriately when `<path>` is outside `areas_root_path`.
    Create `tests/test_listener_lock.py` to test the lockfile mutual exclusion (e.g., locking a mock file and asserting that `run_append_mode` exits cleanly if run concurrently).
  </action>
  <verify>
    <automated>pytest tests/test_main_cli.py tests/test_listener_lock.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert subparser validation behaves as expected.
    - Lockfile test covers the `Timeout` path cleanly.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Command Line Argument → Path Validation | User-provided paths must not traverse outside the `areas_root_path` |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-02 | Tampering / Information Disclosure | src/main.py | high | mitigate | Resolve paths absolutely (`Path.resolve()`) and check using `is_relative_to(config.areas_root_path)`. |
| T-22-SC | Tampering | npm/pip/cargo installs | high | mitigate | `filelock` package audited as legitimate in RESEARCH.md. Ensure explicit version constraints if possible. |
</threat_model>

<verification>
Automated: `pytest tests/test_main_cli.py tests/test_listener_lock.py`
</verification>

<success_criteria>
- The tool can be run as `python main.py create <path>` executing the standard pipeline.
- The tool rejects paths outside `areas_root_path`.
- The tool can be run as `python main.py append` executing the listener stub with lockfile mutual exclusion.
</success_criteria>

<output>
Create `.planning/phases/22-configuration-and-cli-modes/22-02-SUMMARY.md` when done
</output>
