---
phase: "22"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config.yaml
  - src/core/config.py
  - tests/test_core_config.py
autonomous: true
requirements:
  - CONF-01
must_haves:
  truths:
    - User can configure structural paths in a central YAML file.
    - Missing structural directories are auto-created on application startup.
  artifacts:
    - config.yaml
    - src/core/config.py
    - tests/test_core_config.py
  key_links:
    - config.py loads and validates config.yaml using Pydantic.
---

<objective>
Implement central configuration management using a YAML file and Pydantic validation.
Purpose: Externalize structural configuration while keeping secrets in `.env`.
Output: `config.yaml`, `src/core/config.py`, and `tests/test_core_config.py`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-configuration-and-cli-modes/22-CONTEXT.md
</context>

## Artifacts this phase produces

- `config.yaml`: The central configuration file.
- `src/core/config.py`: The Pydantic configuration loader and validator.
- `tests/test_core_config.py`: Unit tests for the configuration module.

<tasks>
<task type="auto">
  <name>Task 1: Add configuration schema and loader</name>
  <files>config.yaml, src/core/config.py</files>
  <read_first>
    - .planning/phases/22-configuration-and-cli-modes/22-PATTERNS.md (Pattern 1: Pydantic Config Loader)
  </read_first>
  <action>
    Implement `AppConfig` Pydantic model in `src/core/config.py` with `inbox_path`, `areas_root_path`, and `area_mappings` (per D-01, D-03). Follow the 'Pydantic BaseModel pattern' from the `src/core/schemas.py` analog.
    Load `config.yaml` using PyYAML and validate via the Pydantic model. Follow the 'YAML Loading and Error Handling' pattern from the `src/tenant_config/yaml_loader.py` analog to raise `ConfigurationError` on malformed YAML.
    Implement automatic directory creation (`Path.mkdir(parents=True, exist_ok=True)`) for `inbox_path` and `areas_root_path` if they do not exist (per D-04).
    Create `config.yaml` at the project root with dummy values (e.g., `inbox_path: ./inbox`). 
    Do not touch `.env`; it remains strictly for secrets (per D-02).
  </action>
  <verify>
    <automated>pytest tests/test_core_config.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `AppConfig.load` parses `config.yaml` and returns a validated Pydantic model.
    - If `config.yaml` has malformed data, it raises an error.
    - Loading the config automatically creates the `inbox_path` and `areas_root_path` on the filesystem.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test configuration loading and directory creation</name>
  <files>tests/test_core_config.py</files>
  <read_first>
    - .planning/phases/22-configuration-and-cli-modes/22-PATTERNS.md (Pattern 3: test_core_config.py)
  </read_first>
  <action>
    Implement unit tests in `tests/test_core_config.py` for the `AppConfig` loading logic. Follow the testing patterns from the `tests/test_tenant_config_yaml_loader.py` analog for testing loading success and missing/malformed yaml.
    Test successful loading from a valid YAML file.
    Test failure on malformed YAML.
    Test that calling `AppConfig.load` successfully executes `mkdir` on the specified `inbox_path` and `areas_root_path`.
  </action>
  <verify>
    <automated>pytest tests/test_core_config.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Test suite passes cleanly.
    - Tests verify the `mkdir` behavior by checking filesystem state using `pytest`'s `tmp_path`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File System → Config | User-provided YAML configuration is parsed by the application |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | src/core/config.py | medium | mitigate | Use `yaml.safe_load()` exclusively; enforce expected schema via Pydantic. |
</threat_model>

<verification>
Automated: `pytest tests/test_core_config.py`
</verification>

<success_criteria>
- Configuration is loaded and validated at startup using PyYAML and Pydantic.
- Required structural directories are created if missing.
- All unit tests for configuration logic pass.
</success_criteria>

<output>
Create `.planning/phases/22-configuration-and-cli-modes/22-01-SUMMARY.md` when done
</output>
