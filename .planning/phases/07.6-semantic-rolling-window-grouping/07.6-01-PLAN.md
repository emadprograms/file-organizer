---
wave: 1
depends_on: []
files_modified:
  - src/schemas.py
  - src/llm.py
  - src/pipeline.py
  - tests/test_semantic_grouping.py
autonomous: true
status: COMPLETED
---

# Phase 07.6: Semantic Grouping & Data Cleaning

## Goal
Overhaul Pass 1.5 to act as a strict Data Cleaning & Interpolation layer (dates and dependent names) and overhaul Pass 2 to group documents using a lightning-fast bulk LLM call based on page summaries.

## Artifacts this phase produces
- `BulkSemanticMatchResult` (class in `src/schemas.py`)
- `check_bulk_semantic_grouping` (function in `src/llm.py`)
- Date interpolation and Dependent alias mapping logic in `src/pipeline.py`
- `tests/test_semantic_grouping.py` (new file)

## Tasks

### 1. Pass 1 Prompt Updates (Hijri Ban & Summary)
<task>
<read_first>
- src/llm.py
- src/schemas.py
</read_first>
<action>
Modify `src/schemas.py` and `src/llm.py`:
1. In `src/llm.py`, update `_build_system_prompt()` to explicitly forbid extracting Hijri dates (e.g., 1445 AH). Instruct it to extract ONLY Gregorian dates, ignoring Hijri dates if both exist.
2. In `src/schemas.py` and `src/llm.py`, update the `summary` field instruction to force a solid, verbose "full story" of the page in one line. Instruct the AI not to be lazy with the summary.
3. Remove all legacy `is_continuation` fields and prompt instructions.
</action>
<acceptance_criteria>
- `grep "Hijri" src/llm.py` shows the ban instruction.
- The `summary` instruction requires a verbose full story.
- `is_continuation` is removed.
</acceptance_criteria>
</task>

### 2. Pass 1.5 Date Cleaning & Interpolation
<task>
<read_first>
- src/pipeline.py
</read_first>
<action>
Modify `src/pipeline.py` (Pass 1.5 logic):
1. Implement a global pass over the extracted dates of all pages.
2. Determine the overarching chronological sequence. Identify `None` dates and extreme outliers (hallucinations/OCR errors).
3. Interpolate the missing/broken dates based on the nearest valid anchors before and after them in the document.
</action>
<acceptance_criteria>
- Date interpolation logic exists in `src/pipeline.py` that handles `None` values and outliers using surrounding context.
</acceptance_criteria>
</task>

### 3. Pass 1.5 Dependent Alias Mapping
<task>
<read_first>
- src/pipeline.py
</read_first>
<action>
Modify `src/pipeline.py` (Pass 1.5 logic):
1. Establish Primary Tenants based on Anchor documents + >3 name mentions.
2. Scan the document for pages categorized as `personal_details`.
3. Extract names from those pages and map them as "aliases" to the active Primary Tenant.
4. Apply this alias mapping so that subsequent documents addressed to the alias route to the Primary Tenant's timeline.
</action>
<acceptance_criteria>
- Alias mapping logic maps names from `personal_details` directly to the Primary Tenant.
</acceptance_criteria>
</task>

### 4. Pass 2 Bulk Semantic Grouping
<task>
<read_first>
- src/pipeline.py
- src/llm.py
- src/schemas.py
</read_first>
<action>
Modify `src/pipeline.py` and `src/llm.py`:
1. In `src/schemas.py`, create `BulkSemanticMatchResult` schema to hold an array of grouped page ranges (e.g., `groups: list[list[int]]`).
2. In `src/llm.py`, create `check_bulk_semantic_grouping(pages_data)` that sends a JSON array of `[page_num, names, summary]` to the local LLM.
3. In `src/pipeline.py`, update `_group_pages_into_documents` to chunk the document into 50-page batches and call `check_bulk_semantic_grouping` to get the exact page groupings.
</action>
<acceptance_criteria>
- Bulk grouping via single/chunked LLM calls replaces the slow sequential sliding window.
- The prompt sends `page_num`, `names`, and `summary` arrays.
</acceptance_criteria>
</task>

### 5. Unit Tests
<task>
<read_first>
- tests/test_semantic_grouping.py
</read_first>
<action>
Create `tests/test_semantic_grouping.py`:
1. Test Date interpolation function with a list of dates containing `None` and an outlier.
2. Test Alias Mapping function with mocked `personal_details` pages.
3. Mock `check_bulk_semantic_grouping` and verify the pipeline groups pages based on the returned ranges.
</action>
<acceptance_criteria>
- Test file covers dates, aliases, and bulk grouping. `pytest` passes.
</acceptance_criteria>
</task>

## Must Haves
truths:
  - D-01: Pass 1 strictly ignores Hijri dates.
  - D-02: Pass 1.5 correctly interpolates `None` dates without needing the LLM.
  - D-03: `personal_details` establishes aliases directly mapping to the Primary Tenant.
  - D-04: Pass 2 groups pages in bulk 50-page chunks (max) using the local LLM, completely eliminating the 1-by-1 sliding window latency.
