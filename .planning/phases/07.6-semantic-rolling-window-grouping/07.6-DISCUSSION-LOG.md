# Phase 07.6: Semantic Rolling Window Grouping - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-25
**Phase:** 07.6-semantic-rolling-window-grouping
**Areas discussed:** Comparison Cost & Latency, Strict Adjacency vs. Interruptions, Summary Strictness, Independent Processing Rule

---

## Comparison Cost & Latency

| Option | Description | Selected |
|--------|-------------|----------|
| Accept latency | Extra LLM call for every adjacent pair | ✓ |
| Use cheaper embedding | Use a smaller model for comparison | |

**User's choice:** Use a local LLM like qwen2.5 14b for this.
**Notes:** Local execution makes the latency/cost of an extra call per pair completely acceptable.

---

## Strict Adjacency vs. Interruptions

| Option | Description | Selected |
|--------|-------------|----------|
| Strict break | Break group on any unrelated/empty page | |
| Carry over | Blank pages or interruptions shouldn't break the group | ✓ |

**User's choice:** If it has a blank page, then that page will have no summary or the summary should be the contents are empty and illegible and it shouldn't break the group.
**Notes:** The rolling window needs to handle explicitly bad/empty pages gracefully without restarting a document.

---

## Summary Strictness

| Option | Description | Selected |
|--------|-------------|----------|
| Strict schema | Enforce JSON fields (Who/What/Topic) strictly | |
| Structured text | Free-form text preferring a given structure | ✓ |

**User's choice:** No don't force the format. Imagine, its just a blank page. just call it what it is. but the structure should be preferred. also the topic shouldn't be one word. it should be a bit verbose in explaining what exactly is going on.
**Notes:** LLM should describe exactly what the page is in a natural but verbose manner.

---

## Independent Processing Rule (Signatures only)

| Option | Description | Selected |
|--------|-------------|----------|
| Strict rule | Process entirely independently and fail | |
| Treat as junk | Such pages are BS and shouldn't disrupt | ✓ |

**User's choice:** If a page just has signature and no other defining text or anything else, then its just bs. what kind of paper only has a signature.
**Notes:** Such "BS" pages should just be flagged naturally and kept in the existing group without disrupting the flow.
