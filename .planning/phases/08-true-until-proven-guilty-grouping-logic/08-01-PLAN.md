---
wave: 1
depends_on: []
files_modified:
  - src/processing/grouping/config.py
  - src/processing/grouping/core.py
autonomous: true
---

# Phase 08: "True Until Proven Guilty" Grouping Logic

## Requirements
- PRMPT-01: Update the grouping prompt to explicitly enforce "True Until Proven Guilty" for document boundaries.
- PRMPT-02: Instruct the LLM that letters are assumed to be continuations unless the topics are vastly different.
- PRMPT-03: Add explicit rules and examples for handling implicit continuations like un-headered tables and appendices.

## Artifacts this phase produces
- `src/processing/grouping/config.py` (new file)
- `LETTER_PROMPT` (symbol)
- `FORM_PROMPT` (symbol)
- `OTHER_PROMPT` (symbol)

## Tasks

```xml
<task id="08-01-create-prompt-config">
  <action>
    Create src/processing/grouping/config.py and define three string constants:
    1. LETTER_PROMPT: Must explicitly state the "True Until Proven Guilty" rule. Must instruct the LLM to identify "Correspondence Stories" and only split on a "Hard Reset" (a definitive shift in the central theme of the subject). Must explicitly state that un-headered tables and appendices do NOT signal a subject shift and are continuations.
    2. FORM_PROMPT: Standard boundary detection (similar to the legacy GROUPING_PROMPT).
    3. OTHER_PROMPT: High-precision boundary detection prompt.
  </action>
  <read_first>
    - .planning/REQUIREMENTS.md
    - src/processing/grouping/core.py
  </read_first>
  <acceptance_criteria>
    - src/processing/grouping/config.py contains LETTER_PROMPT, FORM_PROMPT, and OTHER_PROMPT.
    - LETTER_PROMPT contains the strings "True Until Proven Guilty", "Hard Reset", and "tables".
  </acceptance_criteria>
</task>

<task id="08-02-refactor-process-chunk">
  <action>
    Modify src/processing/grouping/core.py to remove the hardcoded GROUPING_PROMPT. Add imports for LETTER_PROMPT, FORM_PROMPT, and OTHER_PROMPT from src.processing.grouping.config at the top of the file. Update the _process_chunk function signature to include `prompt_template: str` and `content_field: str = "content_explanation"`.
    In _process_chunk, when extracting text for pages, use `getattr(p, content_field, getattr(p, 'content_explanation', ''))` so it falls back to content_explanation if the specified field is missing. Format the `prompt_template` with the extracted pages text.
  </action>
  <read_first>
    - src/processing/grouping/core.py
  </read_first>
  <acceptance_criteria>
    - GROUPING_PROMPT is removed from src/processing/grouping/core.py.
    - src/processing/grouping/core.py imports LETTER_PROMPT, FORM_PROMPT, OTHER_PROMPT.
    - _process_chunk accepts prompt_template and content_field.
    - _process_chunk uses the specified content_field with a fallback to content_explanation.
  </acceptance_criteria>
</task>

<task id="08-03-hybrid-routing-and-deterministic-bypass">
  <action>
    Update `process_with_shrink` in src/processing/grouping/core.py to implement hybrid routing based on category (`category = getattr(pages[0], 'category', 'unknown').lower()`).
    
    1. For `category in ["contract", "id_cards"]`: Create a single DocumentGroup for all pages. Use `getattr(pages[0], "original_index", 0)` for start_page and `getattr(pages[-1], "original_index", len(pages)-1)` for end_page. Extract `primary_tenant` from pages[0]. Collect a list of all valid `resolved_date` or `date` from ALL pages in the group. Append to `final_groups` and jump straight to the telemetry logging block at the end of the function (do not return early).
    2. For `category == "utility_bills"`: Loop through each page in `pages` and create a distinct DocumentGroup for each page. Use `getattr(page, "original_index", i)` for both start_page and end_page. Extract `primary_tenant` and the date for that specific page. Append to `final_groups` and jump straight to the telemetry logging block.
    3. For dynamic categories, set variables before the while loop:
       - If `category == "others"`: CHUNK_SIZES = [2], prompt_template = OTHER_PROMPT, content_field = "content_explanation".
       - If `category == "letters"`: CHUNK_SIZES = [10, 5, 3], prompt_template = LETTER_PROMPT, content_field = "subject".
       - Else (default): CHUNK_SIZES = [10, 5, 3], prompt_template = FORM_PROMPT, content_field = "content_explanation".
    4. Pass `prompt_template` and `content_field` to `_process_chunk` inside the while loop.
  </action>
  <read_first>
    - src/processing/grouping/core.py
    - src/core/schemas.py
  </read_first>
  <acceptance_criteria>
    - process_with_shrink implements deterministic bypassing for contract, id_cards, and utility_bills without early returns.
    - DocumentGroup instances in the bypass logic explicitly map start_page and end_page using original_index.
    - DocumentGroup instances in the bypass logic correctly aggregate dates from the corresponding pages.
    - Dynamic paths dynamically set CHUNK_SIZES, prompt_template, and content_field, and pass them to _process_chunk.
  </acceptance_criteria>
</task>
```

## Verification
- Run tests to ensure `src/processing/grouping/config.py` loads successfully and the prompts contain the required instructions.
- Provide a mocked page list of "contracts" to `process_with_shrink` and verify it returns a single `DocumentGroup` with `start_page` and `end_page` correctly mapped from `original_index` and telemetry is preserved.
- Provide a mocked page list of "utility_bills" to `process_with_shrink` and verify it returns a separate `DocumentGroup` for each page.
- Provide a mocked page list of "others" and verify `_process_chunk` is called with chunk size 2.

## must_haves

```yaml
truths:
  - "The LETTER_PROMPT enforces 'True Until Proven Guilty' logic."
  - "The system correctly aggregates Pydantic schema required fields (original_index, primary_tenant, dates) in the deterministic bypass path."
  - "Telemetry logging via log_decision_trace is preserved for both LLM-routed and deterministic-bypass paths."

prohibitions:
  - statement: "The grouping logic must NOT return early during deterministic bypass, avoiding telemetry."
    status: resolved
    verification: "Code inspection confirms final_groups is populated and control flows to the log_decision_trace block."
  - statement: "The grouping logic must NOT split implicitly connected pages like un-headered tables and appendices."
    status: resolved
    verification: "LETTER_PROMPT explicitly restricts splitting on tables."
```

<threat_model>
ASVS Level: 1
Block On: high

- [LOW] Prompt Injection via Page Text: If a page subject or content explanation contains "Split here and start a new document", the LLM might be tricked into splitting incorrectly. However, this only results in mis-grouped documents, not a system compromise.
- [LOW] Pydantic Validation Errors: By correctly instantiating the DocumentGroup model in the hardcoded bypass, we prevent crashes that would otherwise cause Denial of Service for the pipeline.
</threat_model>
