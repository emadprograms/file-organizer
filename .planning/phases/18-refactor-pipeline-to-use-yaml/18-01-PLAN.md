---
phase: 18
wave: 1
must_haves:
  truths:
    - "Dual-mode strategy correctly bypasses anchor rules when YAML is present (D-08)"
    - "Missing tenant assignment logic prioritizes YAML timelines and handles overlaps (D-09)"
  artifacts: []
  key_links: []
---

# 18-01: YAML Integration and Timeline Logic

## Artifacts this phase produces
- `tests/test_yaml_pipeline.py` (file)
- `tests/test_file_placement.py` (file)

## Task 1: Setup Wave 0 Validation Infrastructure
<read_first>
- .planning/phases/18-refactor-pipeline-to-use-yaml/18-VALIDATION.md
- tests/test_yaml_pipeline.py
- tests/test_file_placement.py
</read_first>

<acceptance_criteria>
- `tests/test_yaml_pipeline.py` is created with empty test stubs for `test_anchor_logic_bypass`, `test_timeline_fallback_overlap`, and `test_timeline_fallback_no_date`.
- `tests/test_file_placement.py` is created with a stub for testing file placements.
- Any necessary mock document/YAML fixtures are added to `tests/conftest.py` or the test files.
- Running `pytest tests/test_yaml_pipeline.py` and `pytest tests/test_file_placement.py` passes (with stubs skipped or trivially passing).
</acceptance_criteria>

<action>
1. Create `tests/test_yaml_pipeline.py` and add stub functions: `test_anchor_logic_bypass`, `test_timeline_fallback_overlap`, `test_timeline_fallback_no_date`.
2. Create `tests/test_file_placement.py` and add a stub function: `test_file_placement_logic`.
3. Add a placeholder fixture for `mock_yaml_config` and `mock_documents` if helpful.
4. Verify the test infrastructure by running the new test files.
</action>

## Task 2: Implement Anchor Logic Bypass and YAML Generation
<read_first>
- src/tenant_config/tenants.py
- src/timeline/phase.py
- tests/test_yaml_pipeline.py
- .planning/phases/18-refactor-pipeline-to-use-yaml/18-CONTEXT.md
</read_first>

<acceptance_criteria>
- The pipeline checks for `tenants.yaml` in the root target directory (D-04).
- **Initial Discovery:** If YAML doesn't exist, it applies the standard anchor rules (>=1 anchor, >=5 pages), extracts timelines, and auto-generates `tenants.yaml` with `name`, `start_date`, and `end_date` (D-05, D-06).
- **Incremental Update:** If YAML exists, the strict anchor logic is skipped entirely (D-08).
- `canonicalize_with_llm` uses the tenant names from `tenants.yaml` to strictly map OCR raw names (D-07).
- Automated tests pass: `pytest tests/test_yaml_pipeline.py::test_anchor_logic_bypass`
</acceptance_criteria>

<action>
1. Implement YAML loading logic in the pipeline, parsing `tenants.yaml` if it is present in the target output directory (D-04).
2. Update `build_tenant_timelines` (or equivalent) in `src/tenant_config/tenants.py` to accept `allowed_tenants`. If `allowed_tenants` is provided, skip the threshold logic (D-08).
3. If `tenants.yaml` is missing, auto-generate it in the target directory based on the discovered timelines, formatting it correctly with `name`, `start_date`, and `end_date` (D-05, D-06).
4. Update `canonicalize_with_llm` to accept the allowed tenant names and prompt the LLM to explicitly restrict mapping to those specific identities (D-07).
5. Implement the actual logic for `test_anchor_logic_bypass` in the test file and ensure it passes.
</action>

## Task 3: Implement Timeline Fallbacks
<read_first>
- src/timeline/phase.py
- tests/test_yaml_pipeline.py
- .planning/phases/18-refactor-pipeline-to-use-yaml/18-CONTEXT.md
</read_first>

<acceptance_criteria>
- Missing tenant assignment logic prioritizes `tenants.yaml` timelines (D-09).
- **Has Date, No Name:** Assigns the document to the tenant whose YAML timeline covers the date. If overlapping, resolves to the **latest** tenant in the overlap (D-10).
- **No Date, No Name:** Assigns the document to the latest overall tenant (marked with `end_date: "present"` in the YAML) (D-11).
- Automated tests pass: `pytest tests/test_yaml_pipeline.py::test_timeline_fallback_overlap` and `pytest tests/test_yaml_pipeline.py::test_timeline_fallback_no_date`
</acceptance_criteria>

<action>
1. Modify the fallback logic in `src/timeline/phase.py` (or where the fallback assignment occurs) to use strictly YAML timelines (D-09).
2. For dates that overlap multiple tenant periods, add a resolution mechanism to pick the latest overlapping tenant based on their dates (D-10).
3. For documents completely lacking dates and names, add a fallback to find the tenant with `end_date: "present"` (or the latest timeline if present is implicitly the latest) (D-11).
4. Implement the actual tests for `test_timeline_fallback_overlap` and `test_timeline_fallback_no_date`.
5. Run the tests to ensure the fallback mechanics operate as specified.
</action>
