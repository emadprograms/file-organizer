---
phase: 18
wave: 2
must_haves:
  truths:
    - ".source_files hiding is correctly implemented (D-02)"
    - "Categorized PDF files are retained in the target root instead of being moved to .source_files (D-01)"
  artifacts: []
  key_links: []
---

# 18-02: File Organization and Final Refactoring

## Artifacts this phase produces
- None

## Task 1: File Placement and .source_files Directory
<read_first>
- src/main.py
- tests/test_file_placement.py
- .planning/phases/18-refactor-pipeline-to-use-yaml/18-CONTEXT.md
</read_first>

<acceptance_criteria>
- The directory for checkpoints and reports is renamed from `source_files` to `.source_files` (D-02).
- The `*_categorized.pdf` output file is kept in the root target directory. It is NOT moved into `.source_files` (D-01).
- Only `*_report.json` and `.run_cache` checkpoints are correctly moved to `.source_files/` (D-03).
- Automated test passes: `pytest tests/test_file_placement.py`
</acceptance_criteria>

<action>
1. Update `src/main.py` where directory paths are constructed for file moving operations at the end of the pipeline.
2. Change the target directory name to `.source_files` (D-02).
3. Adjust the file movement logic so that it excludes `*_categorized.pdf` from the move operations, leaving it in the root output directory (D-01).
4. Ensure only `*_report.json` and `.run_cache` checkpoint files are properly relocated to `.source_files/` (D-03).
5. Implement the actual test logic in `tests/test_file_placement.py` using mock files and directories, then run the test to verify it succeeds.
</action>
