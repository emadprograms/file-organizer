# Plan 88-01: View Mode Switcher & Area Grid Layout

## Tasks
1. Update `src/api/static/index.html`:
   - Add view mode toggle tabs/buttons: "Tree View" and "Grid View".
   - Store and handle `currentViewMode` ('tree' | 'grid').
   - Implement `renderSidebar()` to branch between full tree rendering and area-only list rendering.
   - Add `#area-grid-panel` in the DOM (hidden by default) with responsive grid container.
   - When an area is clicked in Grid View mode, display `#area-grid-panel` and load houses for that area.
2. Verify view switcher toggling and area selection in browser.
