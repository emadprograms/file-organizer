---
phase: "24"
plan: "02"
type: execute
wave: 2
depends_on: ["24-01"]
files_modified: [src/fs_ui/orchestrator.py, src/main.py, tests/test_fs_ui_orchestrator.py]
autonomous: true
requirements: [FSUI-04, FSUI-05, FSUI-06]
must_haves:
  truths:
    - Files are renamed with `_Proposed` to signal filing intention.
    - Files with ` OK` are finalized by triggering the pipeline.
    - Listener is fully stateless, using only filenames to maintain state.
  artifacts:
    - src/fs_ui/orchestrator.py
  key_links:
    - Orchestrator replaces the loop inside `run_append_mode` in `src/main.py`.
---

<objective>
Implement the class-based File-System UI Orchestrator to manage the proposal and finalization lifecycle.

Purpose: Automates the process of inferring missing filing data, proposing filing actions to the user via file renaming, and triggering the functional document pipeline upon user approval.
Output: `FSUIOrchestrator` class and updated `main.py` append mode.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-fs-ui-orchestration/24-RESEARCH.md
@.planning/phases/24-fs-ui-orchestration/24-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FSUIOrchestrator class</name>
  <files>src/fs_ui/orchestrator.py, tests/test_fs_ui_orchestrator.py</files>
  <behavior>
    - Test 1: `process_inbox` delays processing if file size is not stable across two polls.
    - Test 2: `propose` renames a valid file to include `_Proposed.pdf` with fully resolved fields.
    - Test 3: `propose` handles inference errors by renaming to `_Failed.pdf` or `_Error...`.
    - Test 4: `finalize` moves the file and invokes pipeline passes.
  </behavior>
  <action>
    Create `src/fs_ui/orchestrator.py`. Implement `FSUIOrchestrator` class initialized with `config` and `llm_client`. Use `src/pipeline/pipeline.py` as an analog for the class pattern and `tests/test_core_config_parsing.py` for the test structure (per PATTERNS.md).
    Implement `process_inbox(self)`: A stateless `time.sleep(2)` loop. Check all PDFs in `inbox_path`. Track file sizes in a local dict (only track files that do NOT have `_Proposed`, `_Failed`, `_Error`, or ` OK` suffixes to prevent memory bloat); only process a file if its size is `> 0` and hasn't changed since the last poll. Per D-05, ensure this loop logs NO periodic heartbeats (log only on errors or actual processing events).
    Based on filename suffixes, route to:
    - `_Proposed`, `_Failed`, `_Error`: Ignore (waiting for user).
    - ` OK`: Call `finalize(filepath)`.
    - Otherwise: Call `propose(filepath)`.
    
    Implement `propose(self, filepath)`: Move the parsing, inference, area, and tenant resolution logic currently in `main.py`'s `run_append_mode` into this method. After resolving all fields, construct the resolved filename (e.g. `Area House Group Date Title`). Rename the PDF to `{resolved_name}_Proposed.pdf`. If a `_report.json` was generated, rename it too. Catch parsing/conflict errors and rename to `_Error_Invalid_Format.pdf` or `_Failed.pdf` per CONTEXT D-07.
    
    Implement `finalize(self, filepath)`: Strip ` OK` and `_Proposed` to get the clean resolved filename. Move the PDF (and its `_report.json` if it exists) to the correct house directory (into `.source_files/` if appropriate), explicitly using `pathlib.Path.resolve(strict=True)` on the destination path to mitigate path traversal risks (T-24-02-01). Handle name collisions per D-13 by explicitly implementing a timestamp or counter append logic (like the `get_safe_dest` pattern from RESEARCH.md) before moving, to prevent overwrites.
    Crucially, because append mode may skip OCR if the filename is fully specified, explicitly trigger `process_unclassified_pdf` to synthesize `_report.json` if it is missing, ensuring `run_cleaning_pass` has its required OCR payload. Then, invoke the pipeline passes (`run_cleaning_pass`, `run_grouping_pass`, `run_routing_pass`, `run_generation_pass`) for that house ID. Ensure you pass the finalized PDF path to `run_generation_pass` directly so it does not crash. Catch move errors, log them, and abort finalization so the ` OK` suffix remains (per D-12).
  </action>
  <verify>
    <automated>pytest tests/test_fs_ui_orchestrator.py -x</automated>
  </verify>
  <done>The orchestrator successfully processes the inbox, proposes filenames, and handles finalizations statelessly.</done>
</task>

<task type="auto">
  <name>Task 2: Hook Orchestrator into main.py and fix pipeline pass</name>
  <files>src/main.py</files>
  <action>
    Update `run_append_mode` in `src/main.py`.
    Remove the existing `while True` loop and resolution logic (it's now in `FSUIOrchestrator`).
    Remove `FileLock` from the `filelock` package.
    Instead, use `src.fs_ui.lock.acquire_lock` and `release_lock` inside a `try...finally` block. If `LockExistsError` is raised, log a warning and exit gracefully (sys.exit(0)).
    Instantiate `FSUIOrchestrator(config, llm_client)` and call its `process_inbox()` method inside the locked region. Maintain D-05 compliance by ensuring no heartbeats are logged from this setup sequence.
    Also, ensure the `inbox_path` directory is automatically created (`os.makedirs`) before starting the loop (per D-03).
    Additionally, modify `run_generation_pass` (around line 324) to accept an optional `pdf_path: Path | None = None` parameter. Update the file lookup logic so that if `pdf_path` is not provided, it falls back to `list(target_dir.glob(f"{house_id}_categorized*.pdf"))[0]`, ensuring it doesn't crash in append mode where `_categorized.pdf` is not created.
  </action>
  <verify>
    <automated>pytest -x</automated>
  </verify>
  <done>Append mode successfully locks the inbox and starts the FSUIOrchestrator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System → Process | Filenames dictate the filing destination; strict path resolution prevents traversal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-02-01 | Tampering | src/fs_ui/orchestrator.py | medium | mitigate | Use `pathlib`'s strict path resolution when moving files to ensure the destination remains within the allowed `areas_root_path`. |
| T-24-02-02 | Denial of Service | src/fs_ui/orchestrator.py | low | mitigate | Size stability check prevents the listener from reading files that are actively being streamed to the disk, avoiding IO locks. |
</threat_model>

<verification>
Automated tests pass (`pytest tests/test_fs_ui_orchestrator.py`).
</verification>

<success_criteria>
The system proposes filing locations via filename modification and correctly triggers the pipeline upon approval.
</success_criteria>

<output>
Create `.planning/phases/24-fs-ui-orchestration/24-02-SUMMARY.md` when done
</output>
