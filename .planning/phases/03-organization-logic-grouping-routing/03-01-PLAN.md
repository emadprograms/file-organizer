---
wave: 1
depends_on: []
files_modified:
  - src/schemas.py
  - sample-config.yaml
  - tests/test_schemas.py
autonomous: true
---

# Phase 03: Organization Logic (Grouping & Routing)

## Goal
Generalize the second half of the pipeline (Passes 2 and 3) to group and route documents based on the config.

## Requirements
- GRP-01: Update Pass 2 to group pages based on config-defined boundary rules.
- ORG-01: Update Pass 3 to route documents into user-defined "Destination Folders".

## Artifacts this phase produces
- `ConfigGrouping` model (class) in `src/schemas.py`
- `ConfigGrouping.strategy` field in `src/schemas.py`
- `ConfigGrouping.group_by` field in `src/schemas.py`
- `ConfigGrouping.script_path` field in `src/schemas.py`
- `ConfigRouting.strategy` field in `src/schemas.py`
- `ConfigRouting.fallback_folder` field in `src/schemas.py`
- `ConfigRouting.script_path` field in `src/schemas.py`
- `UserConfig.grouping` field in `src/schemas.py`
- `scripts/sample-grouping.py` (new file path)
- `group_pages` function in `scripts/sample-grouping.py`
- `scripts/sample-routing.py` (new file path)
- `organize` function in `scripts/sample-routing.py`
- `tests/test_sample_routing.py` (new file path)

## Tasks

<task>
<action>
Modify `src/schemas.py` to add the `ConfigGrouping` model and update the `ConfigRouting` model. 
1. Create `ConfigGrouping` (BaseModel) with `strategy` (default "declarative"), `group_by` (optional list of strings), and `script_path` (optional string).
2. Add `strategy` (default "template"), `fallback_folder` (default "UNKNOWN"), and `script_path` (optional string) fields to `ConfigRouting`.
3. Add `grouping` field of type `ConfigGrouping` to `UserConfig`.
4. Update `sample-config.yaml` to include `grouping` using `strategy: python` pointing to `./scripts/sample-grouping.py`, and update `routing` to use `strategy: python`, `fallback_folder: UNKNOWN`, and point to `./scripts/sample-routing.py`.
5. Update `tests/test_schemas.py` to verify the new config fields are loaded correctly from `sample-config.yaml`.
</action>
<read_first>
- src/schemas.py
- sample-config.yaml
- tests/test_schemas.py
</read_first>
<acceptance_criteria>
- `src/schemas.py` defines `ConfigGrouping` with `strategy`, `group_by`, and `script_path` fields.
- `src/schemas.py` defines `ConfigRouting` with `strategy`, `fallback_folder`, `script_path`, and `destination_format` fields.
- `UserConfig` explicitly includes `grouping` and `routing`.
- `sample-config.yaml` has `grouping` and `routing` sections utilizing the python strategy and script paths.
- `pytest tests/test_schemas.py` passes.
</acceptance_criteria>
</task>

<task>
<action>
Extract the hardcoded Pass 2 logic into `scripts/sample-grouping.py`.
1. Create `scripts/sample-grouping.py` and implement `def group_pages(raw_pages, client=None) -> list[DocumentGroup]:`.
2. Move the complex boundary logic, LLM bulk semantic grouping (`client.check_bulk_semantic_grouping`), and chunking from `Pipeline._group_pages_into_documents` in `src/pipeline.py` into this new script.
3. Keep the imports (`from src.schemas import DocumentGroup, PageClassification, Category`) and ensure it's self-contained.
</action>
<read_first>
- src/pipeline.py
- src/schemas.py
</read_first>
<acceptance_criteria>
- `scripts/sample-grouping.py` exists and contains a `group_pages` function.
- The `group_pages` function contains the LLM logic `check_bulk_semantic_grouping` and window chunking that previously existed in `src/pipeline.py`.
- Running `python -m py_compile scripts/sample-grouping.py` succeeds with no syntax errors.
</acceptance_criteria>
</task>

<task>
<action>
Extract the hardcoded Pass 3 logic into `scripts/sample-routing.py`.
1. Create `scripts/sample-routing.py` and implement `def organize(documents, source_pdf, output_base_dir) -> dict:`.
2. Move `FileOrganizer._resolve_house_number`, `_compute_tenant_timelines`, `_build_resident_order`, `_generate_pdf_name`, and the full contents of `organize()` from `src/organizer.py` into this new script.
3. Import `from src.schemas import Category, DocumentGroup`, `import src.utils as utils`, and `from src.split import extract_pdf_segment, compress_pdf`.
</action>
<read_first>
- src/organizer.py
- src/schemas.py
</read_first>
<acceptance_criteria>
- `scripts/sample-routing.py` exists and contains an `organize` function.
- The `organize` function contains the tenant numbering, global amar routing, and specialized folder hierarchy previously found in `FileOrganizer`.
- Running `python -m py_compile scripts/sample-routing.py` succeeds with no syntax errors.
</acceptance_criteria>
</task>

<task>
<action>
Refactor Pass 2 grouping logic in `src/pipeline.py`. (D-01)
1. Update `_group_pages_into_documents` signature to `(self, raw_pages, config: 'UserConfig')`. Note: remove `canonical_mapping_clean` parameter as it's no longer needed in Pass 2.
2. Ensure `process_pdf` correctly passes `config` to `_group_pages_into_documents` instead of `canonical_mapping_clean`.
3. If `config.grouping.strategy == "python"`, dynamically load `config.grouping.script_path` using `importlib.util` and execute `grouping_script.group_pages(raw_pages, client=self.client)`.
4. If `config.grouping.strategy == "declarative"`, implement a sequential grouping loop: group contiguous pages that share identical values for all fields listed in `config.grouping.group_by` (defaulting to `["category", "residents"]`), constructing and returning a list of `DocumentGroup` instances.
</action>
<read_first>
- src/pipeline.py
</read_first>
<acceptance_criteria>
- `Pipeline._group_pages_into_documents` uses `config` and drops `canonical_mapping_clean`.
- `_group_pages_into_documents` dynamically loads and executes the python grouping script when strategy is python.
- `_group_pages_into_documents` successfully groups sequential pages based on configurable declarative fields when strategy is declarative.
- `pytest tests/test_pipeline.py` passes (update test mocks if signature changes cause failures).
</acceptance_criteria>
</task>

<task>
<action>
Refactor Pass 3 routing logic in `src/organizer.py` and update `src/main.py`. (D-02, D-03)
1. In `src/organizer.py`, update `FileOrganizer.organize` signature to `(self, documents, source_pdf, output_base_dir, config: 'UserConfig')`.
2. Update `src/main.py` to pass `user_config` to `organizer.organize(...)`.
3. If `config.routing.strategy == "python"`, dynamically load `config.routing.script_path` using `importlib.util` and execute `routing_script.organize(documents, source_pdf, output_base_dir)`.
4. If `config.routing.strategy == "template"`, loop over `documents`. Retrieve the fallback folder if primary tenant is NONE/UNKNOWN, otherwise use `config.routing.destination_format.format(...)` providing `resident`, `category`, and `date`. Ensure the target directory is created and `extract_pdf_segment` is called.
5. In `tests/test_organizer.py`, update tests to use mock `UserConfig` pointing to the template strategy, and verify the template properly formats folder structures, replacing the legacy hardcoded Bahrain tests.
</action>
<read_first>
- src/organizer.py
- src/main.py
- tests/test_organizer.py
</read_first>
<acceptance_criteria>
- `FileOrganizer.organize` uses the config routing strategy.
- `src/main.py` supplies `user_config` to `FileOrganizer.organize`.
- The template strategy resolves dynamic paths correctly based on string interpolation (e.g., `{resident}/{category}`).
- Fallback folder is correctly used when residents are missing.
- `pytest tests/test_organizer.py` passes.
</acceptance_criteria>
</task>

## Verification
- Run `pytest` to ensure all tests pass.
- Verify `Pipeline` and `FileOrganizer` no longer hardcode Bahrain-specific tenant sorting or LLM grouping logic, but rely completely on the `UserConfig` definitions or external python scripts.

## must_haves

```yaml
truths:
  - "D-01: Pass 2 groups pages based on grouping configuration strategies"
  - "D-02: Pass 3 routes documents based on routing configuration template and strategies"
  - "ConfigGrouping model has strategy, group_by, and script_path fields"
  - "ConfigRouting model has strategy, fallback_folder, script_path, and destination_format fields"
  - "UserConfig model explicitly includes grouping and routing"
  - "sample-config.yaml reproduces the existing logic by utilizing the python strategies"
  - "Pipeline._group_pages_into_documents supports declarative grouping using config.grouping.group_by"
  - "Pipeline._group_pages_into_documents supports python script grouping using config.grouping.script_path"
  - "FileOrganizer.organize supports template routing using config.routing.destination_format and fallback_folder"
  - "FileOrganizer.organize supports python script routing using config.routing.script_path"
  - "D-03: Fallback folder is correctly used when residents are missing or documents are unclassified"
```
