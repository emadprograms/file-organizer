# Plan 93-01: Legacy Data Migration & Storage Restructuring

## Tasks
1. TDD Phase 1 - Write Migration Tests:
   - Create `tests/test_migration_v11.py` with mock legacy house fixture (complete with state.json, report.json, vault PDFs, tenant YAML, shortcuts).
   - Test `migrate_house_to_v11`:
     - Verifies area, house, tenant, batch, pages, and documents created in DB.
     - Verifies vault PDFs preserved in `{house_id}/vault/`.
     - Verifies raw PDF moved to `{house_id}/batches/`.
     - Verifies legacy shortcuts and JSONs removed cleanly.
     - Verifies idempotency: second run succeeds with 0 errors and no duplicate rows.
   - Test dry-run mode: verifies zero disk modifications.
2. Implement Migration Module:
   - Create `src/migration/v11_migration.py`:
     - `migrate_house_to_v11(house_dir, area_id, db_path, dry_run=False, rename_folder=True)`
     - `migrate_areas(areas_root_path, db_path, area_mappings=None, dry_run=False)`
     - Verification/integrity checks ensuring every DB document has an accessible file on disk.
3. Integrate CLI command:
   - Expose `migrate-v11` CLI command in `src/main.py` with dry-run and verbose flags.
4. Run full test suite and verify 100% pass.
