# Phase 07.5.1: Hybrid Cloud-First Vision Extraction with Local Overflow - Patterns

## Files to be Created / Modified

### 1. `src/llm.py`
**Role:** Core LLM routing and rate-limiting orchestrator.
**Data Flow:** Contains the `GemmaClient` and state tracking mechanisms. It evaluates current API capacity and routes `extract_page` requests to the cloud API or offloads to the local model (e.g., Qwen-VL) based on the sliding window cooldown timer.
**Closest Analog:** The existing rate-limiting tracker (`global_rpm_tracker`) and the `_get_client_and_key()` method in `GemmaClient`.
**Concrete Code Excerpt:**
```python
    def extract_page(self, image_bytes: bytes) -> str:
        last_error = None
        
        # Cloud-First OCR with Local Overflow
        client_info = self._get_client_and_key(estimated_tokens=500, no_wait=True)
        if client_info:
            client, key, reserve_time = client_info
            try:
                import time
                start_time = time.time()
                print("  [Cloud OCR] Gemini bucket available. Extracting via gemini-1.5-flash...")
                text = self._extract_text_with_gemini(client, image_bytes)
                self._reconcile_usage(key, reserve_time, 500) # approximate
                return text
            except Exception as e:
                print(f"  [Cloud OCR Failed] Falling back to local Qwen-VL: {e}")
                self._report_failure(key, is_429=("429" in str(e)))
        else:
            print("  [Rate Limit Overflow] Gemini bucket empty. Offloading to local Qwen-VL...")
```

### 2. `src/gui.py` (Optional / Expected Update)
**Role:** Displays real-time metrics and system state to the user.
**Data Flow:** Consumes from the `telemetry_queue` populated by `llm.py` and updates UI indicators to reflect whether the system is in "Cloud" mode or "Local Overflow" mode.
**Closest Analog:** The `poll_telemetry` method handling telemetry updates.
**Concrete Code Excerpt:**
```python
    def poll_telemetry(self):
        try:
            while True:
                state = self.telemetry_queue.get_nowait()
                # Update UI to show system mode (e.g., "Local Overflow" vs "Cloud Active")
```

### 3. `tests/test_vision_manager.py` (To be Created)
**Role:** Validation of state management and seamless handoff.
**Data Flow:** Simulates API limits and timeout/429 exceptions to test the internal state machine tracking the 15 rpm limit, asserting that the fallback and recovery behavior operates strictly within Pass 1a as expected.
**Closest Analog:** Existing tests like `test_vision.py` or `test_name_match.py`.
