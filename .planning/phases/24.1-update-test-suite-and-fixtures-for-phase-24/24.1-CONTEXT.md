# Phase 24.1: update-test-suite-and-fixtures-for-phase-24 - Context

**Gathered:** 2026-07-23
**Status:** Ready for planning

<domain>
## Phase Boundary

Refactoring the automated test suite to point to the newly created authentic mock house fixtures (Safra C 1273 and Safra D 504) instead of the old, removed test structures, and ensuring all E2E assertions map to the real documents.

</domain>

<decisions>
## Implementation Decisions

### Assertion Strategy
- **D-01:** Hardcode expectations. Assert against the exact final folder paths (like `.../05_عقود/`) using a static dictionary of expected outputs.
- **D-02:** LLM responses for the new inbox PDFs will be captured once and saved as pre-saved `_report.json` mock files to keep the assertions deterministic.

### State/Continuity Mocking
- **D-03:** Unaffected. `tests/test_pipeline_e2e_continuity.py` uses an isolated `continuity_test_state.json` mock array that does not depend on the physical folder structure. No changes needed to the state file itself.

### Inbox Tests Mapping
- **D-04:** Targeted Tests. Map each new inbox PDF to a specific test function (e.g., `test_fallback_inference()`, `test_success_flow()`). Ensure every possible edge case and scenario is covered by a dedicated test, rather than a single sequential run.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

No external specs — requirements fully captured in decisions above.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `tests/fixtures/e2e/golden_state/inbox/`: New mock PDFs are located here.
- `tests/fixtures/e2e/golden_state/areas/`: Authentic mock house structures (1273 and 504) are located here.

### Established Patterns
- LLM mocking via pre-saved `_report.json` files for deterministic E2E tests.

</code_context>

<specifics>
## Specific Ideas

No specific requirements — open to standard approaches

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 24.1-update-test-suite-and-fixtures-for-phase-24*
*Context gathered: 2026-07-23*
