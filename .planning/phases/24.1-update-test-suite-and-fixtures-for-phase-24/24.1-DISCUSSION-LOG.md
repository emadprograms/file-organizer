# Phase 24.1: update-test-suite-and-fixtures-for-phase-24 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-23
**Phase:** 24.1-update-test-suite-and-fixtures-for-phase-24
**Areas discussed:** Assertion Strategy, State/Continuity Mocking, Inbox Tests Mapping

---

## Assertion Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| (Recommended) Hardcode expectations. | Assert against the exact final folder paths (like `.../05_عقود/`) using a static dictionary of expected outputs. This keeps tests simple and highly deterministic. | ✓ |
| Dynamically infer expectations. | Re-implement the parsing logic inside the test to calculate the final destination path from the raw PDF. More flexible but risks mirroring production bugs. | |
| You decide! | Whatever you think is most stable. | |

**User's choice:** Fair enough, I actually like that idea. I like reading the pre-saved report.json and mocking the LLM call we are going to stick with hard coding the numbers
**Notes:** Clarified that LLM calls are mocked and bypassed for determinism.

---

## State/Continuity Mocking

| Option | Description | Selected |
|--------|-------------|----------|
| (Recommended) Generate a new static state snapshot. | We will run the pipeline once on the new 1273 data, save the intermediate JSON as the new `continuity_test_state.json`, and keep the test ultra-fast. | |
| Generate the state dynamically. | Rewrite the test to run the first half of the pipeline in a `pytest` fixture, then test the continuity. Slower test execution, but no need to maintain static JSON states. | |
| You decide! | Whatever is best for the test suite. | |

**User's choice:** I don't think that you are correct because we are still maintaining the golden 1273 folder we have made a separate golden state folder for you you know for E2E testing so I think that continuity test state.json should not be affected can you confirm what I'm saying is correct or not
**Notes:** User correctly identified that the `continuity_test_state.json` file is completely independent of the physical mock folders and is just an arbitrary mock array. No action needed for this area.

---

## Inbox Tests Mapping

| Option | Description | Selected |
|--------|-------------|----------|
| Targeted Tests. | Create separate, individual test functions for each file to isolate failures. | ✓ |
| Sequential E2E Run. | Drop all PDFs into the inbox at once and run a single loop, mimicking real life but harder to debug. | |

**User's choice:** Let's stick with option one, but make sure that we are hitting every case possible. make sure that we are testing every edge case and we have a test for every edge case, it does not need to be a sequential run. but make sure that we test every scenario.
**Notes:** User wants targeted tests, but heavily emphasized that every edge case must have its own test.

---

## the agent's Discretion

None

## Deferred Ideas

None
