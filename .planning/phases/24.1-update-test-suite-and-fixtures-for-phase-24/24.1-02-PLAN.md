---
plan_id: 24.1-02
phase: 24.1
wave: 2
depends_on: ["24.1-01"]
files_modified:
  - tests/test_e2e_fs_ui.py
  - tests/test_pipeline_e2e.py
autonomous: true
requirements: []
---

# Objective
Update the core E2E tests to use the new authentic fixtures, inject the deterministic mock reports, and assert hardcoded expected paths instead of fuzzy checking standard output.

# Must Haves
- `test_e2e_fs_ui.py` must run offline (no API key required) by injecting the mock reports created in Plan 01.
- Assertions must check the final disk structure directly (e.g., ensuring a finalized PDF goes to `Safra C/1273.../07_استقطاع إيجار/`).
- `test_pipeline_e2e.py` must test the `create` mode CLI against the new 1273 and 504 mock areas.

<tasks>
<task>
<task_id>24.1-02-T01</task_id>
<title>Refactor FS UI E2E Tests to use Mock Reports</title>
<read_first>
- tests/test_e2e_fs_ui.py
</read_first>
<action>
Remove the `pytestmark = pytest.mark.skipif(not has_api_key)` logic from `tests/test_e2e_fs_ui.py`.
In `e2e_workspace` or specific tests, configure the LLM client to use a dummy key.
Modify the test suite to use dedicated, targeted test functions for each inbox PDF, replacing the old sequential tests (`test_e2e_fs_ui_grouping_flag`, `test_e2e_fs_ui_hardcoded_group`, etc.):
- `test_fsui_infer_missing_data_1273()`: Uses `SAFC 1273 U G U.pdf` and its mock report to test fallback inference.
- `test_fsui_missing_yaml_abort()`: Uses `SAFC 1273 يونس 7 U.pdf` to test FSUI-06 missing YAML abort on 1273.
- `test_fsui_infer_unknown_area_1273()`: Uses `U 1273 U U U.pdf` and its mock report.
- `test_fsui_success_flow_504()`: Uses `U 504 U U U.pdf` and its mock report.
- `test_fsui_broken_syntax()`: Uses `broken.pdf` to ensure proper fallback/error handling.

For each test (except broken), copy the corresponding `_report.json` from `tests/fixtures/e2e/golden_state/reports/` to the inbox alongside the target PDF immediately before calling `orchestrator.propose()`.
Assert the final output paths explicitly using hardcoded strings (e.g., checking if the PDF moved to the expected tenant and category folder).
</action>
<acceptance_criteria>
- `pytest tests/test_e2e_fs_ui.py` passes without setting `GEMINI_API_KEY`.
- No `skipif` decorator is present in `tests/test_e2e_fs_ui.py`.
- `tests/test_e2e_fs_ui.py` contains exactly the 5 new targeted test functions listed above.
- `tests/test_e2e_fs_ui.py` contains explicit hardcoded assertions like `assert (house_dir / "يونس محمد ملاك ‎(2002 - الآن)‎" / "01_بيانات أساسية" / "expected.pdf").exists()`.
</acceptance_criteria>
<verify>
Run `pytest tests/test_e2e_fs_ui.py -v`.
</verify>
</task>

<task>
<task_id>24.1-02-T02</task_id>
<title>Refactor Pipeline E2E CLI Tests</title>
<read_first>
- tests/test_pipeline_e2e.py
</read_first>
<action>
Rewrite `tests/test_pipeline_e2e.py` to test the `main.py create` command.
Remove `_setup_run_dir` using the old `golden_1273` structure.
Create two new tests: `test_cli_create_1273` and `test_cli_create_504`.
In each test, use `shutil.copytree()` to copy the respective area from `tests/fixtures/e2e/golden_state/areas/` to the `tmp_path`.
Run `main.py create <path_to_copied_house>` via `sys.argv` mocking or subprocess.
Assert that the command exits successfully and that the output structure inside the copied directory matches expectations (for example, generating `1273_3_routed_and_finalized.json`).
Ensure tests run with `--skip-llm` or offline configs.
</action>
<acceptance_criteria>
- `tests/test_pipeline_e2e.py` does not reference `golden_1273/input/` anymore.
- `tests/test_pipeline_e2e.py` has `test_cli_create_1273` and `test_cli_create_504`.
- `pytest tests/test_pipeline_e2e.py` passes offline.
</acceptance_criteria>
<verify>
Run `pytest tests/test_pipeline_e2e.py -v`.
</verify>
</task>
</tasks>

# Artifacts this phase produces
- Modified `tests/test_e2e_fs_ui.py`
- Modified `tests/test_pipeline_e2e.py`
