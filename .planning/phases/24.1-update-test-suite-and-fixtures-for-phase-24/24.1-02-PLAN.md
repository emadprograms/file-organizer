---
plan_id: 24.1-02
phase: 24.1
wave: 2
depends_on: ["24.1-01"]
files_modified:
  - tests/test_e2e_fs_ui.py
  - tests/test_pipeline_e2e.py
  - src/categorization/categorization.py
autonomous: true
requirements: []
---

# Objective
Update the core E2E tests to use the new authentic fixtures, inject the deterministic mock reports properly, and test the `append` mode logic against finalized golden houses while retaining a `create` mode E2E test.

# Must Haves
- `test_e2e_fs_ui.py` must run offline (no API key required) by correctly injecting the mock reports created in Plan 01 into the `.tmp_{stem}_master/` hidden directories.
- Assertions must check the final disk structure directly (e.g., ensuring a finalized PDF goes to `Safra C/1273.../07_استقطاع إيجار/`).
- `test_pipeline_e2e.py` must test the `append` CLI mode logic on the 1273 and 504 mock areas, and retain a `create` mode E2E test.
- `categorization.py` must skip `_finalized.pdf` files to prevent massive slow-downs if an archive is scanned.

<tasks>
<task>
<task_id>24.1-02-T01</task_id>
<title>Refactor FS UI E2E Tests to use Mock Reports securely</title>
<read_first>
- tests/test_e2e_fs_ui.py
- src/categorization/categorization.py
</read_first>
<action>
Remove the `pytestmark = pytest.mark.skipif(not has_api_key)` logic from `tests/test_e2e_fs_ui.py`.
In `e2e_workspace` or specific tests, configure the LLM client to use a dummy key.
Modify the test suite to use dedicated, targeted test functions for each inbox PDF:
- `test_fsui_infer_missing_data_1273()`: Uses `SAFC 1273 U G U.pdf` and its mock report.
- `test_fsui_missing_yaml_abort()`: Uses `SAFC 1273 يونس 7 U.pdf` to test FSUI-06 missing YAML abort on 1273.
- `test_fsui_infer_unknown_area_1273()`: Uses `U 1273 U U U.pdf` and its mock report.
- `test_fsui_success_flow_504()`: Uses `U 504 U U U.pdf` and its mock report.
- `test_fsui_broken_syntax()`: Uses `broken.pdf` to ensure proper fallback/error handling.

For each test (except broken), pre-create the hidden directory `.tmp_{stem}_master/` inside the inbox for the target PDF, and copy the corresponding `_report.json` from `tests/fixtures/e2e/golden_state/reports/` directly into this hidden directory immediately before calling `orchestrator.propose()`. This ensures `categorization.py` bypasses the LLM successfully.
Assert the final output paths explicitly using hardcoded strings.
</action>
<acceptance_criteria>
- `pytest tests/test_e2e_fs_ui.py` passes without setting `GEMINI_API_KEY`.
- The test suite creates `.tmp_{stem}_master/` directories and places `_report.json` inside them.
- `tests/test_e2e_fs_ui.py` contains exactly the 5 new targeted test functions listed above.
</acceptance_criteria>
<verify>
Run `pytest tests/test_e2e_fs_ui.py -v`.
</verify>
</task>

<task>
<task_id>24.1-02-T02</task_id>
<title>Refactor Pipeline E2E CLI Tests for Create and Append Modes</title>
<read_first>
- tests/test_pipeline_e2e.py
- src/cli/main.py
</read_first>
<action>
Rewrite `tests/test_pipeline_e2e.py` to test the `main.py append` command logic and retain at least one test for `main.py create`.
Remove `_setup_run_dir` using the old `golden_1273` structure.
Create new tests: `test_cli_create_mode` (using the 1273 setup as a baseline), `test_cli_append_1273`, and `test_cli_append_504`.
In each test:
1. Use `shutil.copytree()` to copy the respective area from `tests/fixtures/e2e/golden_state/areas/` to an `areas_root` in `tmp_path` (for append mode) or copy raw inputs for create mode.
2. Create an `inbox` directory in `tmp_path`.
3. Copy an appropriate inbox PDF (e.g. `SAFC 1273 U G U.pdf`) into the `inbox`.
4. Pre-create the `.tmp_{stem}_master/` directory and inject the mock `_report.json` (similar to T01).
5. Explicitly use `unittest.mock.patch` to set `src.llm.llm.LLMClient.skip_llm = True` to ensure the new instance created by `append` does not make network calls.
6. Mock `time.sleep` in `src.fs_ui.orchestrator.time.sleep` to raise a `SystemExit(0)` to break out of the `process_inbox` infinite loop after one pass. Ensure the test handles this `SystemExit` cleanly (e.g., using `pytest.raises(SystemExit)`).
7. Run `main.py` (append or create) via `sys.argv` mocking.
8. Assert that the command processes the inbox file, creates the proposed PDF or finalizes it, and exits smoothly due to the mock.
</action>
<acceptance_criteria>
- `tests/test_pipeline_e2e.py` does not reference `golden_1273/input/` anymore.
- `tests/test_pipeline_e2e.py` tests both `main.py create` and `main.py append`.
- `LLMClient.skip_llm` is explicitly mocked to `True`.
- `SystemExit` is correctly handled to break the infinite loop.
- `pytest tests/test_pipeline_e2e.py` passes offline.
</acceptance_criteria>
<verify>
Run `pytest tests/test_pipeline_e2e.py -v`.
</verify>
</task>

<task>
<task_id>24.1-02-T03</task_id>
<title>Skip Finalized PDFs in Categorization</title>
<read_first>
- src/categorization/categorization.py
</read_first>
<action>
Modify `process_unclassified_pdf` in `src/categorization/categorization.py`.
Currently, around line 48, it skips `_categorized` PDFs:
`if "_categorized" in pdf_path.name:`
Update this logic to also skip `_finalized` PDFs:
`if "_categorized" in pdf_path.name or "_finalized" in pdf_path.name:`
This system improvement guarantees that massive `_finalized.pdf` archives are never accidentally re-processed as raw inputs.
</action>
<acceptance_criteria>
- `src/categorization/categorization.py` contains the updated `if "_categorized" in pdf_path.name or "_finalized" in pdf_path.name:` logic.
</acceptance_criteria>
<verify>
Inspect `src/categorization/categorization.py` to confirm the change.
</verify>
</task>
</tasks>

# Artifacts this phase produces
- Modified `tests/test_e2e_fs_ui.py`
- Modified `tests/test_pipeline_e2e.py`
- Modified `src/categorization/categorization.py`
