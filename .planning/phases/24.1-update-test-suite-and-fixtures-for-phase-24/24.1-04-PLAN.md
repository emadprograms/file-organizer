---
plan_id: 24.1-04
phase: 24.1
wave: 4
depends_on: ["24.1-01", "24.1-02", "24.1-03"]
files_modified:
  - tests/test_pipeline_e2e.py
autonomous: true
requirements: []
---

# Objective
Update `inject_mock_report` and `test_cli_append_504` in `tests/test_pipeline_e2e.py` so that the mock category and assertions accurately reflect the real LLM output (`letters` category, tenant `أحمد يوسف المريسل`, date `2021-05-24`).

# Must Haves
- `inject_mock_report` in `tests/test_pipeline_e2e.py` generates `category: letters`, `date: 2021-05-24`, `expected_tenant_name: أحمد يوسف المريسل`, and `expected_house_number: 504` when handling house `504`.
- `test_cli_append_504` in `tests/test_pipeline_e2e.py` asserts the updated proposed filename matching `letters` category routing and schema (`Safra D 504 أحمد يوسف المريسل 13 2021-05-24 عنوان تجريبي_Proposed.pdf`).
- Running `pytest tests/test_pipeline_e2e.py` executes cleanly and passes all test cases.

<tasks>
<task>
<task_id>24.1-04-T01</task_id>
<title>Update inject_mock_report and test_cli_append_504 in test_pipeline_e2e.py</title>
<read_first>
- tests/test_pipeline_e2e.py
- .planning/phases/24.1-update-test-suite-and-fixtures-for-phase-24/24.1-UAT.md
</read_first>
<action>
In `tests/test_pipeline_e2e.py`:
1. Modify `inject_mock_report()` so that if `"504"` is in `stem`:
   - `category` is set to `"letters"`
   - `date` is set to `"2021-05-24"`
   - `expected_tenant_name` is set to `"أحمد يوسف المريسل"`
   - `expected_house_number` is set to `"504"`
2. Update `test_cli_append_504()` filename assertion to expect:
   `"Safra D 504 أحمد يوسف المريسل 13 2021-05-24 عنوان تجريبي_Proposed.pdf"`.
</action>
<acceptance_criteria>
- `inject_mock_report` produces `category: letters`, `date: 2021-05-24`, and tenant `أحمد يوسف المريسل` for stem `U 504 U U U`.
- `test_cli_append_504` asserts proposed file name `"Safra D 504 أحمد يوسف المريسل 13 2021-05-24 عنوان تجريبي_Proposed.pdf"`.
- `pytest tests/test_pipeline_e2e.py` passes 100% green.
</acceptance_criteria>
<verify>
Run `pytest tests/test_pipeline_e2e.py -v`.
</verify>
</task>
</tasks>
