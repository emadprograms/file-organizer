---
plan_id: 24.1-03
phase: 24.1
wave: 3
depends_on: ["24.1-01"]
files_modified:
  - tests/test_pipeline_e2e_default.py
  - tests/test_pipeline_e2e_others.py
autonomous: true
requirements: []
---

# Objective
Migrate the grouping logic edge-case tests to use the new deterministic `_report.json` mock files instead of the deprecated `pdfs/1273/567_report.json` path.

# Must Haves
- The tests must point to `tests/fixtures/e2e/golden_state/reports/` for loading JSON data.
- The targeted pages for testing must exist in the mock JSON files provided in Wave 1.

<tasks>
<task>
<task_id>24.1-03-T01</task_id>
<title>Migrate test_pipeline_e2e_default.py</title>
<read_first>
- tests/test_pipeline_e2e_default.py
- tests/fixtures/e2e/golden_state/reports/ (expected to exist from 24.1-01)
</read_first>
<action>
Change `json_path` in `tests/test_pipeline_e2e_default.py` from `"pdfs/1273/567_report.json"` to `"tests/fixtures/e2e/golden_state/reports/U 504 U U U_report.json"`.
Adjust `target_pages` to match valid pages present in the new `U 504 U U U_report.json` mock file (e.g., `"1"`, `"2"`, `"3"`).
Replace all occurrences of `.get()` lookups or string indices on the loaded JSON data with integer list indexing, e.g., `page_data = data[int(p_idx)]`, because the new mock files are JSON arrays, not dictionaries.
Update the assertions to match the logic of grouping the pages from the new mock file.
</action>
<acceptance_criteria>
- `json_path` points to `"tests/fixtures/e2e/golden_state/reports/U 504 U U U_report.json"`.
- All `.get()` and string-based index lookups on the JSON array are removed and replaced with integer indexing `data[int(p_idx)]`.
- `pytest tests/test_pipeline_e2e_default.py` runs and passes.
</acceptance_criteria>
<verify>
Run `pytest tests/test_pipeline_e2e_default.py -v`.
</verify>
</task>

<task>
<task_id>24.1-03-T02</task_id>
<title>Migrate test_pipeline_e2e_others.py</title>
<read_first>
- tests/test_pipeline_e2e_others.py
- tests/fixtures/e2e/golden_state/reports/ (expected to exist from 24.1-01)
</read_first>
<action>
Change `json_path` in `tests/test_pipeline_e2e_others.py` to point to a valid mock report, such as `"tests/fixtures/e2e/golden_state/reports/U 504 U U U_report.json"`.
Adjust the page loop (`range(30, 34)`) to iterate over pages actually present in the chosen mock report, simulating an "others" category scenario.
Replace all occurrences of `.get()` lookups or string indices on the loaded JSON data with integer list indexing, e.g., `page_data = data[int(p_idx)]`, because the new mock files are JSON arrays.
Ensure `mock_process_chunk.return_value` is updated to align with the new mock page indexes.
</action>
<acceptance_criteria>
- `json_path` points to a file in `tests/fixtures/e2e/golden_state/reports/`.
- All `.get()` and string-based index lookups on the JSON array are removed and replaced with integer indexing `data[int(p_idx)]`.
- The test mocks a grouping operation accurately corresponding to the selected pages.
- `pytest tests/test_pipeline_e2e_others.py` runs and passes.
</acceptance_criteria>
<verify>
Run `pytest tests/test_pipeline_e2e_others.py -v`.
</verify>
</task>
</tasks>

# Artifacts this phase produces
- Modified `tests/test_pipeline_e2e_default.py`
- Modified `tests/test_pipeline_e2e_others.py`
