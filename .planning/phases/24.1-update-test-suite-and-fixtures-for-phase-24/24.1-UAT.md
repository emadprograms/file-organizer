---
status: diagnosed
phase: 24.1-update-test-suite-and-fixtures-for-phase-24
source: 24.1-01-SUMMARY.md, 24.1-02-SUMMARY.md, 24.1-03-SUMMARY.md
started: 2026-07-23T23:17:00Z
updated: 2026-07-23T23:48:45Z
---

## Current Test

[testing complete]

## Tests

### 1. E2E Append Mode Test Suite
expected: Running `pytest tests/test_pipeline_e2e.py` passes completely without hitting the live Gemini API, and asserts the 5 targeted append CLI modes correctly.
result: issue
reported: "the output it returned was Failed? it is supposed to give the correct output and rename it to Proposed? is it not? ... I wish to know to know if is successfully generating the 'correct' output. I want a real llm call and I want the correct output."
severity: major

### 2. Full Test Suite Run
expected: Running `pytest` on the full suite runs all 257 tests and passes green without requiring LLM API dependencies.
result: pass

## Summary

total: 2
passed: 1
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Running `pytest tests/test_pipeline_e2e.py` passes completely without hitting the live Gemini API, and asserts the 5 targeted append CLI modes correctly."
  status: failed
  reason: "User reported: the output it returned was Failed? it is supposed to give the correct output and rename it to Proposed? is it not? ... I wish to know to know if is successfully generating the 'correct' output. I want a real llm call and I want the correct output."
  severity: major
  test: 1
  root_cause: "In `tests/test_pipeline_e2e.py`, `inject_mock_report()` hardcodes `category: contract` for `U 504 U U U.pdf` with synthetic date `2015-05-05`, whereas real LLM output classifies it as `letters` with tenant `أحمد يوسف المريسل` and dates `2021-05-24`, causing a mismatch in routing and assertion."
  artifacts:
    - path: "tests/test_pipeline_e2e.py"
      issue: "inject_mock_report hardcodes contract instead of realistic LLM letters schema"
  missing:
    - "Update test mock fixture and assertion in test_cli_append_504 to match real LLM classification (`letters` category)"
