# Plan 94-01: Ingestion Pipeline Redesign

## Tasks
1. TDD Phase 1 - Write Ingestion Tests:
   - Create `tests/test_ingest_v11.py` with mock LLM client and synthetic multi-page PDF.
   - Test single-batch ingest:
     - Verifies `batches` row created with status='completed'.
     - Verifies `pages` rows created with OCR fields, fine category, and resolved tenant.
     - Verifies sliced vault PDFs created in `{house}/vault/doc_{vault_id}.pdf`.
     - Verifies `documents` rows inserted with correct metadata and `batch_id`.
     - Verifies `pages.vault_id` foreign keys populated.
     - Verifies NO `.lnk` shortcuts or `.json` state files created.
   - Test multi-batch / prepend ingest:
     - Ingest a second PDF for the same house.
     - Verifies new batch row and new page numbers (1..M) created.
     - Verifies older batch rows, page rows, and document rows are 100% UNTOUCHED (zero index shifting!).
     - Verifies queries ordered by `primary_date DESC` correctly order across batches.
2. Implement v11 Ingest Engine:
   - Create `src/ingest/v11_ingest.py`:
     - `ingest_pdf_to_house(pdf_path, house_id, area_id, db_path, areas_root, llm_client, ...)`
     - Integrates with `Repository`.
   - Update `src/main.py` ingest mode to support `--v11` or default to database-backed ingestion.
3. Run full test suite (`pytest tests/test_ingest_v11.py tests/test_migration_v11.py tests/db/`) and verify 100% pass.
