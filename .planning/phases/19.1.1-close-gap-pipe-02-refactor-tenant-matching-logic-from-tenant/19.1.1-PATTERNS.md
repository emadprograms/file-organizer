# Phase 19.1.1 Patterns & Analogies

## 1. `src/grouping/name_matcher.py` (New File)
**Role:** Tenant matching and canonicalization logic.
**Data Flow:** Takes raw extracted names from pages, normalizes Arabic text, groups them into fuzzy clusters, and then uses the LLM to canonicalize those clusters into strict identities based on `allowed_tenants` or general Arabic conventions.
**Closest Analog:** `src/grouping/utils.py` (utility/helper functions)
**Concrete Excerpt:**
```python
# To be moved from src/tenant_config/tenants.py
def normalize_arabic_text(text: str) -> str: ...
def cluster_names_fuzzily(names: set[str]) -> dict[str, str]: ...
def canonicalize_with_llm(unresolved_names: list[str], llm_client: LLMClient, allowed_tenants: list[str] = None) -> dict[str, str]: ...
```

## 2. `src/timeline/timeline_builder.py` (New File)
**Role:** Generates active timelines for each canonical tenant.
**Data Flow:** Iterates over `PageData`, matches with canonical names, aggregates dates, checks for required page/anchor counts (or relies on `allowed_tenants`), and produces a list of `TenantTimeline` models.
**Closest Analog:** `src/timeline/dates.py` or domain logic in `src/timeline/core.py`.
**Concrete Excerpt:**
```python
# To be moved from src/tenant_config/tenants.py
from src.core.models import PageData, TenantTimeline

def build_tenant_timelines(pages: list[PageData], canonical_mapping: dict[str, str], allowed_tenants: list[str] = None) -> list[TenantTimeline]: ...
```

## 3. `src/tenant_config/tenants.py` (Modify)
**Role:** Legacy module / Dumping ground being cleaned out.
**Data Flow:** Logic removed to establish strict domain boundaries according to Phase 16 architecture. 
**Concrete Excerpt:**
The functions `normalize_arabic_text`, `cluster_names_fuzzily`, `canonicalize_with_llm`, and `build_tenant_timelines` will be deleted from here.

## 4. `src/timeline/phase.py` (Modify)
**Role:** Phase 1 (Document Cleaning) execution logic.
**Data Flow:** Currently imports the refactored functions from `src.tenant_config.tenants`. Needs to be updated to import them from their new locations.
**Concrete Excerpt:**
```diff
- from src.tenant_config.tenants import (
-     cluster_names_fuzzily, 
-     canonicalize_with_llm, 
-     build_tenant_timelines
- )
+ from src.grouping.name_matcher import cluster_names_fuzzily, canonicalize_with_llm
+ from src.timeline.timeline_builder import build_tenant_timelines
```
