# Phase 19.1.1 Discussion Log

## Topic: Module Entrypoints
**Options Presented:**
1. Create a single `match_names()` function that runs both fuzzy clustering and LLM matching internally.
2. Keep them separate: `pipeline.py` calls fuzzy clustering, then LLM matching separately.

**User Selection:** Keep them separate.
**Notes:** User specifically requested to keep them separate for modularity.

## Topic: Timeline Generation
**Options Presented:**
1. Move `build_tenant_timelines` to `timeline/` to match the Phase 16 architecture.
2. Keep it in `tenant_config/tenants.py` for now.

**User Selection:** Move to `timeline/`.
**Notes:** User agreed that moving it consolidates timeline logic and cleans up the architecture.
