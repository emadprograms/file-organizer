---
phase: 02-pipeline-adaptation-extraction-cleaning
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/schemas.py
  - src/pipeline.py
  - sample-config.yaml
autonomous: true
requirements:
  - EXT-02
must_haves:
  truths:
    - Pass 1.5 executes configured Python script or LLM prompt instead of hardcoded logic
  artifacts:
    - ConfigCleaning section in UserConfig
  key_links:
    - UserConfig -> Pipeline._run_cleaning_pass
---

<objective>
Refactor Pass 1.5 to use user-defined cleaning rules (LLM or Python) instead of hardcoded logic.

Purpose: D-03 requires Pass 1.5 to be fully configurable.
Output: Dynamic cleaning dispatcher in `pipeline.py`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pipeline-adaptation-extraction-cleaning/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Cleaning Configuration Schema</name>
  <files>src/schemas.py, sample-config.yaml</files>
  <behavior>
    - `UserConfig` includes a `cleaning` section with `strategy: "llm" | "python"`.
    - Includes fields for `prompt_template` (if LLM) or `script_path` (if Python).
  </behavior>
  <action>
Update `UserConfig` in `src/schemas.py` to add a `cleaning: ConfigCleaning` field. `ConfigCleaning` should support selecting a strategy ("llm" or "python") and contain the relevant instruction (either a prompt or a path to a Python file). Update `sample-config.yaml` to include a sample LLM cleaning prompt that replicates the date and alias logic (e.g. outlier detection and alias mapping) per D-03.
  </action>
  <verify>
    <automated>python -c "import yaml; from src.schemas import UserConfig; UserConfig(**yaml.safe_load(open('sample-config.yaml')))"</automated>
  </verify>
  <done>UserConfig perfectly loads the cleaning fields from sample-config.yaml without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace Hardcoded Pass 1.5 Logic</name>
  <files>src/pipeline.py</files>
  <behavior>
    - Pipeline skips `_interpolate_dates` and `_map_aliases`.
    - Pipeline instead delegates to a dynamic `_run_cleaning_pass` based on the config.
  </behavior>
  <action>
In `src/pipeline.py`, remove or deprecate the hardcoded `_interpolate_dates` and `_map_aliases` logic. Implement a `_run_cleaning_pass` that inspects the configuration. If the strategy is "python", dynamically load and execute the Python script specified in the config, passing the raw pages. If the strategy is "llm", bundle the raw pages and send them to `LLMClient` with the configured cleaning prompt. 
  </action>
  <verify>
    <automated>python -m pytest || echo "MISSING — Wave 0 must create test_pipeline.py first"</automated>
  </verify>
  <done>Pipeline executes Pass 1.5 dynamically via the configuration strategy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config→Pipeline | User configuration specifies a Python script to execute |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Execution | script_path | medium | accept | As a local CLI tool running on the user's machine, providing the ability to run local scripts is an intended feature; the user is trusted. |
</threat_model>

<verification>
Execute the pipeline and verify that the hardcoded logic is replaced by the sample-config.yaml cleaning rules.
</verification>

<success_criteria>
Pass 1.5 uses configured logic instead of hardcoded functions.
</success_criteria>

<output>
Create `.planning/phases/02-pipeline-adaptation-extraction-cleaning/02-02-SUMMARY.md` when done
</output>
