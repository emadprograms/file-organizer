---
phase: 02-pipeline-adaptation-extraction-cleaning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas.py
  - src/extractors.py
  - src/llm.py
  - src/pipeline.py
  - sample-config.yaml
autonomous: true
requirements:
  - EXT-01
must_haves:
  truths:
    - Pass 1 uses the user's exact prompt from the config
    - The LLM's response is validated against a Pydantic model generated dynamically from the config's fields
  artifacts:
    - Updated UserConfig schema supporting dynamic fields
    - Updated LLMClient logic for dynamic model generation
  key_links:
    - UserConfig -> CloudExtractor -> LLMClient (passing dynamic schema & prompt)
---

<objective>
Update Pass 1 to extract metadata dynamically based on the configuration file rather than hardcoded schemas.

Purpose: D-01 (Full control prompt) and D-02 (Strict Schema dynamic model) require the pipeline to abandon hardcoded assumptions in favor of config-driven extraction.
Output: Extractors and LLM clients using dynamic schemas.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pipeline-adaptation-extraction-cleaning/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Configuration Schema for Dynamic Fields</name>
  <files>src/schemas.py, sample-config.yaml</files>
  <behavior>
    - `ConfigExtraction` accepts a `prompt_template` and a list of `fields` (name, type, description).
    - `sample-config.yaml` correctly specifies the four required fields (residents, category, date, summary) matching the old behavior.
  </behavior>
  <action>
Update `ConfigExtraction` in `src/schemas.py` to hold `prompt_template: str` and `fields: list[ConfigField]`. Define `ConfigField` to hold `name`, `type` (e.g., "string", "list[str]"), and `description`. Update `sample-config.yaml` to include these fields and the prompt template per D-01 and D-02. Leave `PageClassification` in place for backwards compatibility in Pass 1.5 for now.
  </action>
  <verify>
    <automated>python -c "import yaml; from src.schemas import UserConfig; UserConfig(**yaml.safe_load(open('sample-config.yaml')))"</automated>
  </verify>
  <done>UserConfig perfectly loads the extraction fields from sample-config.yaml without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Dynamic Schema Extraction in LLM Client</name>
  <files>src/llm.py, src/extractors.py, src/pipeline.py</files>
  <behavior>
    - `LLMClient.classify_page_direct` builds a Pydantic model at runtime using `pydantic.create_model` based on the provided fields.
    - Uses the configured `prompt_template` instead of a hardcoded string.
  </behavior>
  <action>
Update `LLMClient.classify_page_direct` to accept `prompt_template: str` and `fields: list[ConfigField]`. Generate a Pydantic model at runtime using `pydantic.create_model` mapping string types from `ConfigField` to actual Python types (D-02). Modify `CloudExtractor.extract` to take these arguments. Update `Pipeline.process_pdf` in `src/pipeline.py` to load `UserConfig` from the environment or a parameter, and pass the config extraction details to `CloudExtractor`. (Change type hint in pipeline to `Any` or `BaseModel` for the returned page classification object since it's dynamic).
  </action>
  <verify>
    <automated>python -m pytest || echo "MISSING — Wave 0 must create test_llm.py first"</automated>
  </verify>
  <done>LLM client successfully structures output using dynamic fields generated from config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserConfig→LLM | User-provided prompts and schema sent to external LLM provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Injection | prompt_template | medium | mitigate | Pydantic validation ensures the returned format is structured; user is trusted for local CLI |
</threat_model>

<verification>
Run the pipeline on a dummy PDF and verify it uses the sample-config.yaml to extract fields.
</verification>

<success_criteria>
Pass 1 completely delegates its prompt and schema definition to the loaded configuration.
</success_criteria>

<output>
Create `.planning/phases/02-pipeline-adaptation-extraction-cleaning/02-01-SUMMARY.md` when done
</output>
