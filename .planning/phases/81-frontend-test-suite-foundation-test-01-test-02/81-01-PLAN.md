---
phase: 81
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.js
  - tests/frontend/setup.js
  - tests/frontend/components/ui.test.js
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - Vitest test suite runs successfully
    - JSDOM environment is configured
    - Initial UI component test verifies basic DOM rendering
  artifacts:
    - package.json
    - vitest.config.js
    - tests/frontend/setup.js
    - tests/frontend/components/ui.test.js
  key_links:
    - vitest.config.js sets environment to jsdom
---

<objective>
Initialize the frontend test suite using Vitest and JSDOM, and implement an initial UI component test.

Purpose: To establish a fast and reliable testing foundation for frontend components before implementing the read-only web GUI.
Output: A configured Vitest test environment and a passing initial DOM test.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-frontend-test-suite-foundation-test-01-test-02/81-AI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize NPM and configure Vitest</name>
  <read_first>
    - package.json
    - vitest.config.js
    - .planning/phases/81-frontend-test-suite-foundation-test-01-test-02/81-AI-SPEC.md
  </read_first>
  <files>package.json, vitest.config.js</files>
  <action>
    Initialize a `package.json` in the project root if it doesn't exist using `npm init -y`.
    Install vitest, jsdom, and @testing-library/dom as dev dependencies using `npm install -D vitest jsdom @testing-library/dom`.
    Update `package.json` to include a `test:frontend` script that runs `vitest run`.
    Create `vitest.config.js` at the project root setting `environment: 'jsdom'` and configuring `setupFiles: ['./tests/frontend/setup.js']`.
  </action>
  <verify>
    <automated>npm run test:frontend -- --help</automated>
  </verify>
  <done>
    `package.json` contains `vitest` in devDependencies and a `test:frontend` script. `vitest.config.js` is created and exports a configuration with `environment: 'jsdom'`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement initial UI test</name>
  <read_first>
    - tests/frontend/setup.js
    - tests/frontend/components/ui.test.js
    - .planning/phases/81-frontend-test-suite-foundation-test-01-test-02/81-AI-SPEC.md
  </read_first>
  <files>tests/frontend/setup.js, tests/frontend/components/ui.test.js</files>
  <behavior>
    - Test 1: DOM rendering works in JSDOM (creating a div, setting text content, and verifying it).
  </behavior>
  <action>
    Create the `tests/frontend/setup.js` file for global test setup (can be left empty for now).
    Create `tests/frontend/components/ui.test.js` using Vitest `describe`, `it`, and `expect`.
    Implement a basic test that mounts a DOM element (`document.body.innerHTML = '<div>Hello World</div>'`), selects it, and asserts its `textContent` is 'Hello World'.
    Ensure `document.body.innerHTML = ''` is called in an `afterEach` block for cleanup.
  </action>
  <verify>
    <automated>npm run test:frontend</automated>
  </verify>
  <done>
    The test suite executes successfully and verifies basic DOM API functionality in the JSDOM environment.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/CD Pipeline | Untrusted code could execute during tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-01 | Tampering | npm installs | high | mitigate | Use exact versions or package-lock.json to prevent dependency confusion |
| T-81-SC | Tampering | npm installs | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<artifacts_this_phase_produces>
- tests/frontend/setup.js
- tests/frontend/components/ui.test.js
- vitest.config.js
- package.json
</artifacts_this_phase_produces>

<verification>
Vitest test suite must execute cleanly in the JSDOM environment.
</verification>

<success_criteria>
Frontend test suite structure is established, dependencies are installed, and an initial test passes successfully.
</success_criteria>

<output>
Create `.planning/phases/81-frontend-test-suite-foundation-test-01-test-02/81-01-SUMMARY.md` when done
</output>
