---
phase: 07-anti-hallucination-schema-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/processing/routing/router.py
  - tests/test_routing_schema.py
autonomous: true
requirements:
  - SCHM-01
must_haves:
  truths:
    - RoutingResponse.model_validate raises ValidationError when selected_folder is not in the provided context list.
    - RoutingResponse.model_validate succeeds when selected_folder is in the provided context list.
  artifacts:
    - src/processing/routing/router.py
    - tests/test_routing_schema.py
  key_links:
    - Pydantic @field_validator -> info.context['allowed_folders']
---

<objective>
Implement the strict Pydantic schema for routing responses to prevent hallucinations.

Purpose: Move validation from post-parsing manual checks to structural enforcement within the Pydantic model using a dynamic field validator.
Output: A validated RoutingResponse model and a dedicated test suite for schema enforcement.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-anti-hallucination-schema-enforcement/07-CONTEXT.md
@.planning/phases/07-anti-hallucination-schema-enforcement/07-RESEARCH.md
@src/processing/routing/router.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement RoutingResponse with field validator</name>
  <files>src/processing/routing/router.py</files>
  <behavior>
    - The RoutingResponse model must have a @field_validator for 'selected_folder'.
    - The validator must access 'allowed_folders' from info.context.
    - If the value is not in the list, it must raise a ValueError (which Pydantic wraps in ValidationError).
  </behavior>
  <action>
    Update RoutingResponse in src/processing/routing/router.py. 
    Replace the current static model with one that uses @field_validator('selected_folder').
    The validator should check if v is in info.context.get('allowed_folders', []) and raise ValueError if not.
  </action>
  <verify>
    <automated>pytest tests/test_routing_schema.py</automated>
  </verify>
  <done>RoutingResponse correctly enforces allowed_folders via validation context.</done>
</task>

<task type="auto">
  <name>Task 2: Create routing schema unit tests</name>
  <files>tests/test_routing_schema.py</files>
  <action>
    Create tests/test_routing_schema.py.
    Implement tests that use RoutingResponse.model_validate(data, context={'allowed_folders': [...]}) to verify:
    1. Valid folder names pass.
    2. Invalid folder names raise pydantic.ValidationError.
    3. Missing context defaults to an empty list (causing failure for any input).
  </action>
  <verify>
    <automated>pytest tests/test_routing_schema.py</automated>
  </verify>
  <done>Schema enforcement is verified by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LLM -> Pydantic | Untrusted JSON output from LLM is validated against the schema |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | RoutingResponse | medium | mitigate | Use @field_validator to ensure LLM cannot inject arbitrary folder names. |
| T-07-SC | Tampering | npm/pip installs | high | mitigate | No new packages installed. |
</threat_model>

<verification>
Ensure that the validator does not rely on any global state and only uses the provided context.
</verification>

<success_criteria>
RoutingResponse strictly rejects any folder name not present in the provided validation context.
</success_criteria>

<output>
Create `.planning/phases/07-anti-hallucination-schema-enforcement/07-01-SUMMARY.md` when done
</output>
