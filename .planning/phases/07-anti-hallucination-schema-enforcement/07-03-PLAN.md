---
phase: 07-anti-hallucination-schema-enforcement
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - tests/test_routing.py
autonomous: true
requirements:
  - SCHM-01
must_haves:
  truths:
    - Integration tests confirm that invalid folders trigger the retry loop.
    - Integration tests confirm that 3 consecutive invalid folders result in a hard exception.
    - Valid folders still route correctly in a single attempt.
  artifacts:
    - tests/test_routing.py
  key_links:
    - End-to-end routing flow -> Schema Validator -> Retry Logic
---

<objective>
Perform a full integration audit and update the routing test suite to ensure stability and correctness.

Purpose: Verify that the new schema enforcement and retry logic work together across the pipeline without regressions.
Output: A comprehensive test suite covering success, retry, and failure modes of the routing phase.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-anti-hallucination-schema-enforcement/07-CONTEXT.md
@src/processing/routing/router.py
@tests/test_routing.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Routing Integration Tests</name>
  <files>tests/test_routing.py</files>
  <action>
    Update tests/test_routing.py to include:
    1. A test case where the LLM returns an invalid folder twice, then a valid one (should pass on 3rd attempt).
    2. A test case where the LLM returns invalid folders 3 times (should raise RoutingValidationError).
    3. A test case verifying that the feedback prompt is actually constructed and passed to the LLM client during retries.
    4. Regression tests for existing valid routing scenarios.
  </action>
  <verify>
    <automated>pytest tests/test_routing.py</automated>
  </verify>
  <done>Routing integration tests cover all success and failure paths.</done>
</task>

<task type="auto">
  <name>Task 2: Full Routing Integration Audit</name>
  <files>[]</files>
  <action>
    Run the full routing test suite and a sample e2e run on a small set of documents to ensure that:
    1. No documents are being mis-routed due to schema changes.
    2. The logs correctly capture validation failures and retries.
    3. The system performance is not significantly impacted by the validation overhead.
  </action>
  <verify>
    <automated>pytest tests/test_routing.py tests/test_routing_schema.py</automated>
  </verify>
  <done>Integration audit complete; routing is stable and hallucination-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| System -> Tests | Tests must accurately simulate LLM hallucinations to verify the retry loop. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Information Disclosure | Logs | low | accept | Validation errors in logs are for auditing and do not leak secrets. |
| T-07-SC | Tampering | npm/pip installs | high | mitigate | No new packages installed. |
</threat_model>

<verification>
Cross-reference the number of attempts in logs with the expected count (1, 2, or 3).
</verification>

<success_criteria>
The routing system is proven to reject hallucinations via structural validation and handles retries/failures as specified.
</success_criteria>

<output>
Create `.planning/phases/07-anti-hallucination-schema-enforcement/07-03-SUMMARY.md` when done
</output>
