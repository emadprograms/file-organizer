---
phase: 07-anti-hallucination-schema-enforcement
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/llm/providers.py
  - src/llm/llm.py
  - src/processing/routing/router.py
autonomous: true
requirements:
  - SCHM-01
must_haves:
  truths:
    - LLMProvider.generate passes validation_context to model_validate.
    - route_document performs exactly 3 attempts before failing.
    - Attempts 2 and 3 include "rejected value" feedback in the prompt.
    - Exhausting 3 attempts raises a hard exception.
  artifacts:
    - src/llm/providers.py
    - src/llm/llm.py
    - src/processing/routing/router.py
  key_links:
    - LLMClient -> LLMProvider -> Pydantic model_validate(..., context=...)
---

<objective>
Integrate the dynamic schema validator into the routing pipeline and implement the 3-attempt feedback loop.

Purpose: Ensure the LLM is corrected when it hallucinations a folder name, and stop the pipeline if it fails repeatedly.
Output: Updated LLM orchestration and routing logic with structural enforcement and resilient retries.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-anti-hallucination-schema-enforcement/07-CONTEXT.md
@.planning/phases/07-anti-hallucination-schema-enforcement/07-RESEARCH.md
@src/processing/routing/router.py
@src/llm/llm.py
@src/llm/providers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update LLM Provider Protocol and Implementations</name>
  <files>src/llm/providers.py</files>
  <action>
    1. Update LLMProvider protocol's generate method to include `validation_context: dict | None = None`.
    2. Update GeminiProvider, OpenRouterProvider, and GroqProvider.generate to use `response_schema.model_validate(data, context=validation_context)` instead of `response_schema(**data)`.
  </action>
  <verify>
    <automated>pytest tests/test_routing_schema.py</automated>
  </verify>
  <done>LLM providers now support passing validation context to Pydantic models.</done>
</task>

<task type="auto">
  <name>Task 2: Update LLMClient to propagate validation context</name>
  <files>src/llm/llm.py</files>
  <action>
    Update LLMClient.generate_content and LLMClient._route_llm_call to accept `validation_context: dict | None = None` as an argument and pass it to the provider's generate method.
  </action>
  <verify>
    <automated>pytest tests/test_routing_schema.py</automated>
  </verify>
  <done>Validation context is correctly propagated from the client to the provider.</done>
</task>

<task type="auto">
  <name>Task 3: Implement 3-attempt Routing Loop with Feedback</name>
  <files>src/processing/routing/router.py</files>
  <action>
    1. Define a custom exception `RoutingValidationError(Exception)` for hard failures.
    2. Rewrite route_document:
       - Initialize `attempts = 3`.
       - In the loop, pass `validation_context={'allowed_folders': allowed_folders}` to llm_client.generate_content.
       - If a pydantic.ValidationError (or ValueError from validator) occurs:
         - Catch the error.
         - On attempt 2 and 3, append a feedback message to the prompt: "This is a retry. Your previous response '[X]' was rejected because it was not in the allowed list. Please choose strictly from: [Allowed Folders]."
         - Increment attempt counter.
       - If the 3rd attempt fails, raise `RoutingValidationError`.
    3. Ensure the fallback to '13_others' is only used for non-validation failures if appropriate, or remove the silent fallback for validation errors as per requirements.
  </action>
  <verify>
    <automated>pytest tests/test_routing.py</automated>
  </verify>
  <done>Routing loop enforces strict schema validation with feedback and hard failure after 3 attempts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LLM -> Router | Feedback loop informs LLM of failures, but Router must still validate every response. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Denial of Service | Routing Loop | low | accept | 3-attempt limit prevents infinite loops. |
| T-07-SC | Tampering | npm/pip installs | high | mitigate | No new packages installed. |
</threat_model>

<verification>
Verify that the prompt updated for retry actually contains the value that caused the failure.
</verification>

<success_criteria>
Routing now uses structural validation via providers, and the pipeline halts with a RoutingValidationError after 3 unsuccessful attempts.
</success_criteria>

<output>
Create `.planning/phases/07-anti-hallucination-schema-enforcement/07-02-SUMMARY.md` when done
</output>
