---
phase: 05-global-logger-migration
plan: 04
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03]
files_modified: []
autonomous: false
requirements: 
  - REQ-LOG-01
  - REQ-LOG-03
must_haves:
  truths:
    - app.log contains 0 entries from external libraries (Isolation).
    - app.log is plain text; debug.log is JSONL (Dual-Format).
    - All application logs use the "file_organizer.module" hierarchy.
  artifacts:
    - Validated app.log, debug.log, and traces.jsonl from a full run.
  key_links:
    - codebase -> logging system -> validated output files.
---

<objective>
Perform a final end-to-end audit of the logging system to verify isolation, formatting, and hierarchical naming.

Purpose: Ensure that the migration has not introduced regressions and that the logging system meets all functional requirements defined in the Logging Overhaul.
Output: Verification report confirming the system is isolated, structured, and unified.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-global-logger-migration/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-End Pipeline Execution</name>
  <files>[]</files>
  <action>
    Execute a full end-to-end run of the file organizer pipeline on a sample directory.
    Run it twice: once with `verbose=False` and once with `verbose=True`.
  </action>
  <verify>
    <automated>
      python src/organize.py tests/fixtures/golden_1273
    </automated>
  </verify>
  <done>Pipeline completes without crashes and generates the run directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Log File Audit</name>
  <what-built>The complete global logger migration across src/ and tests/.</what-built>
  <how-to-verify>
    1. Open the most recent run directory in `logs/`.
    2. Check `app.log`: 
       - Confirm it is plain text.
       - Confirm every line starts with `file_organizer.` (or is the root `file_organizer` logger).
       - Confirm NO logs from `openai`, `google-genai`, `urllib3`, etc.
    3. Check `debug.log`:
       - Confirm it is valid JSONL.
       - Confirm it contains `DEBUG` level logs.
    4. Check `traces.jsonl`:
       - Confirm structured LLM traces are present.
    5. Check Console Output:
       - Confirm `verbose=False` only showed high-level progress.
       - Confirm `verbose=True` showed detailed UI elements.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No trust boundaries crossed.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Logs | low | accept | All logs are local to the run directory. |
</threat_model>

<verification>
Successful human verification of the log files and console output confirms that all requirements are met.
</verification>

<success_criteria>
- Verified Isolation: 0 external library logs in app.log.
- Verified Formats: Text app.log, JSONL debug.log.
- Verified Hierarchy: All logs use "file_organizer.module".
- Verified Verbosity: Console output respects the verbose flag.
</success_criteria>

<output>
Create `.planning/phases/05-global-logger-migration/05-04-SUMMARY.md` when done
</output>
