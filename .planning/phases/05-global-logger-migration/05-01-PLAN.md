---
phase: 05-global-logger-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - src/**/*.py
  - tests/**/*.py
autonomous: true
requirements: 
  - REQ-LOG-04
must_haves:
  truths:
    - Every module in src/ and tests/ uses the hierarchical logger pattern.
    - Logger variable name is standardized to 'logger'.
  artifacts:
    - Updated .py files with corrected logger instantiations.
  key_links:
    - Module loggers -> "file_organizer" root logger.
---

<objective>
Migrate all logger instantiations in the codebase to a strict hierarchical pattern and standardize the logger variable name.

Purpose: Ensure all logs are correctly categorized by module and follow a consistent naming convention, facilitating better filtering and debugging.
Output: codebase where every module uses `logger = logging.getLogger(f"file_organizer.{__name__}")`.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-global-logger-migration/05-CONTEXT.md
@C:/Users/Emad/Documents/GitHub/file-organizer/src/logger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize Loggers in src/</name>
  <files>src/**/*.py</files>
  <action>
    Update all logger instantiations in the `src/` directory to follow the strict hierarchical pattern per D-01 and D-03.
    
    1. Search for `logging.getLogger("file_organizer")`, `logging.getLogger(__name__)`, or any variable named `log = ...`.
    2. Replace them with `logger = logging.getLogger(f"file_organizer.{__name__}")`.
    3. Ensure the variable name is always `logger`.
    4. Update all subsequent calls from `log.info()` to `logger.info()`, etc.
    
    Exclude `src/logger.py` from this specific transformation where it manages the root or "file_organizer" base logger.
  </action>
  <verify>
    <automated>
      # Check that the pattern is used and no old patterns remain in src/
      grep -r 'logging.getLogger(f"file_organizer.{__name__}")' src/
      # The following should return 0 matches (excluding logger.py)
      # grep -r "logging.getLogger("file_organizer")" src/ --exclude=logger.py
    </automated>
  </verify>
  <done>All modules in src/ use the hierarchical logger pattern with the variable name 'logger'.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize Loggers in tests/</name>
  <files>tests/**/*.py</files>
  <action>
    Apply the same hierarchical logger migration to the `tests/` directory per D-04.
    
    1. Replace any local logger instantiations (e.g., `logging.getLogger("test_logger")`) with `logger = logging.getLogger(f"file_organizer.{__name__}")`.
    2. Standardize variable names to `logger`.
  </action>
  <verify>
    <automated>
      grep -r 'logging.getLogger(f"file_organizer.{__name__}")' tests/
    </automated>
  </verify>
  <done>All tests use the same hierarchical logger pattern as the source code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No trust boundaries crossed. This is internal refactoring.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Logging | low | accept | Loggers only capture system state; no secrets are logged in this phase. |
</threat_model>

<verification>
Confirm that all `.py` files in `src/` and `tests/` (excluding `logger.py`) use exactly one logger instantiation: `logger = logging.getLogger(f"file_organizer.{__name__}")`.
</verification>

<success_criteria>
- All loggers are hierarchical.
- All logger variables are named `logger`.
- No regressions in existing logging functionality.
</success_criteria>

<output>
Create `.planning/phases/05-global-logger-migration/05-01-SUMMARY.md` when done
</output>
