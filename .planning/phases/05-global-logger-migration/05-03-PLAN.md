---
phase: 05-global-logger-migration
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: 
  - src/**/*.py
autonomous: true
requirements: 
  - REQ-LOG-03
must_haves:
  truths:
    - rich.console is used only for primary user-facing flow.
    - UI output verbosity is synced with the global 'verbose' flag.
  artifacts:
    - Updated src files with standardized console usage.
  key_links:
    - verbose flag -> console output visibility.
---

<objective>
Clarify the boundary between system telemetry (logging) and user-facing CLI output (Rich console) and sync UI verbosity with the global flag.

Purpose: Ensure the user sees only what is necessary for the flow, while maintaining the ability to see detailed UI output when requested, without cluttering the console with system logs.
Output: codebase where user-facing output is strictly `console.print` and filtered by the `verbose` flag.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-global-logger-migration/05-CONTEXT.md
@C:/Users/Emad/Documents/GitHub/file-organizer/src/logger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize User-Facing Output</name>
  <files>src/**/*.py</files>
  <action>
    Audit all `rich.console` usage to ensure it is used ONLY for primary user-facing flow (progress, results, critical errors) per D-09 and D-10.
    
    1. Identify any `console.print` calls that look like telemetry (e.g., "Processing file X...").
    2. Move these telemetry calls to `logger.info()`.
    3. Ensure all remaining user-facing outputs use `console.print` for consistent formatting.
  </action>
  <verify>
    <automated>
      # Manual review of console.print calls in src/
      grep -r "console.print" src/
    </automated>
  </verify>
  <done>User-facing output is strictly separated from system telemetry.</done>
</task>

<task type="auto">
  <name>Task 2: Sync Console Verbosity with Verbose Flag</name>
  <files>src/organize.py, src/processing/**/*.py</files>
  <action>
    Implement a mechanism to sync `rich.console` output with the global `verbose` flag per D-11.
    
    Suggested Implementation:
    1. In `src/organize.py`, where the `console` object is instantiated or passed around, ensure it respects the `verbose` flag.
    2. Create a simple helper or wrap `console.print` such that "detailed" UI output (e.g., debug trees or tables) only prints if `verbose` is True.
    3. Update `src/processing/visualizer.py` and other UI-heavy modules to use this conditional printing.
  </action>
  <verify>
    <automated>
      # This requires manual verification by running the app with -v and without.
      # The executor should document the specific UI elements that are now conditional.
    </automated>
  </verify>
  <done>Console output verbosity is controlled by the global verbose flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No trust boundaries crossed.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Information Disclosure | Console | low | accept | UI output only contains non-sensitive processed data. |
</threat_model>

<verification>
Run the application with `verbose=False` and verify that only high-level progress is shown. Run with `verbose=True` and verify that detailed UI elements (like trees/tables) are visible.
</verification>

<success_criteria>
- Strict separation: Telemetry $ightarrow$ Logger, UI $ightarrow$ Console.
- `verbose=False` hides detailed UI elements.
- `verbose=True` shows all UI elements.
</success_criteria>

<output>
Create `.planning/phases/05-global-logger-migration/05-03-SUMMARY.md` when done
</output>
