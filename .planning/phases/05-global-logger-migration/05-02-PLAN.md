---
phase: 05-global-logger-migration
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: 
  - src/**/*.py
autonomous: true
requirements: 
  - REQ-LOG-04
must_haves:
  truths:
    - No system-level print() calls remain in src/.
    - Telemetry is exclusively handled by the logger.
  artifacts:
    - Modified src files with print() replaced by logger methods.
  key_links:
    - telemetry output -> logger -> log files.
---

<objective>
Convert all system telemetry `print()` statements in the `src/` directory to appropriate logging levels.

Purpose: Unify all system output into the logging framework to ensure consistency, allow for different verbosity levels, and capture stack traces in `debug.log`.
Output: codebase where telemetry is handled by `logger.info()`, `logger.warning()`, `logger.error()`, or `logger.exception()`.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-global-logger-migration/05-CONTEXT.md
@.planning/phases/05-global-logger-migration/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Telemetry Prints to Loggers</name>
  <files>src/**/*.py</files>
  <action>
    Exhaustively replace `print()` calls in `src/` that are used for system state, warnings, or errors with logger calls per D-05, D-06, and D-07.
    
    1. Map `print(f"ERROR: ...")` or `print(f"UNEXPECTED ERROR: ...")` $ightarrow$ `logger.exception(...)` or `logger.error(...)` to ensure stack traces are captured.
    2. Map `print(f"Warning: ...")` $ightarrow$ `logger.warning(...)`.
    3. Map general state updates $ightarrow$ `logger.info(...)`.
    4. Specifically handle cases where `file=sys.stderr` is used by mapping them to `logger.error()` or `logger.warning()`.
    
    Ignore `console.print()` calls as they are for user-facing UI (handled in Plan 03).
  </action>
  <verify>
    <automated>
      # Search for remaining print( calls in src/ that are NOT console.print
      # This is a conceptual check; the executor should verify no non-UI prints remain.
      grep -r "print(" src/ | grep -v "console.print"
    </automated>
  </verify>
  <done>All telemetry print() calls in src/ are converted to logger calls.</done>
</task>

<task type="auto">
  <name>Task 2: Final Print Purge Verification</name>
  <files>src/**/*.py</files>
  <action>
    Perform a final scan of `src/` to ensure no system-level `print` statements were missed (D-08). 
    Any remaining `print()` must be explicitly justified as non-telemetry (which is unlikely in this project).
  </action>
  <verify>
    <automated>
      # The result should only contain console.print or 0 matches.
      grep -r "print(" src/ | grep -v "console.print"
    </automated>
  </verify>
  <done>Verified that no system-level print statements remain in the src directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No trust boundaries crossed.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Information Disclosure | Logging | low | accept | Standard logging levels used. |
</threat_model>

<verification>
Run a search for `print(` in `src/` and confirm that every match is a `console.print()` call.
</verification>

<success_criteria>
- 0 telemetry print() calls in src/.
- All system events are logged via the hierarchical logger.
- Error prints now include full context via logger.exception().
</success_criteria>

<output>
Create `.planning/phases/05-global-logger-migration/05-02-SUMMARY.md` when done
</output>
