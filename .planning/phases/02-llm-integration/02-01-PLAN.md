---
title: "Schema Definition & LLM Client Refactor"
phase: 02
wave: 1
depends_on: []
files_modified:
  - src/llm.py
  - src/schemas.py
  - requirements.txt
requirements:
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04
autonomous: true
---

# Plan 01 — Schema Definition & LLM Client Refactor

## Goal

Refactor the existing `GemmaClient.process_image()` method into a structured `classify_page()` method that sends page images (multimodal vision) to the `gemma-4-31b-it` model, returns native structured JSON output via Pydantic schemas, and implements robust retry logic with exponential backoff and key rotation. Gemma reads the scanned page images directly — no separate OCR step is needed.

## must_haves

- [ ] `Category` enum with exactly 13 members defined in `src/schemas.py`
- [ ] `PageClassification` Pydantic model with `house_number`, `resident`, `category`, `is_continuation` fields
- [ ] `GemmaClient.classify_page()` sends page images (not text) to `gemma-4-31b-it` using multimodal vision
- [ ] Native JSON schema mode via `response_mime_type="application/json"` + `response_schema=PageClassification`
- [ ] `ThinkingConfig(thinking_budget=0)` disables thinking mode
- [ ] Retry decorator with exponential backoff (min=4s, max=60s) and max 7 attempts
- [ ] Key rotation on 429/rate-limit errors
- [ ] `pydantic` added to `requirements.txt`

## Artifacts this phase produces

### New file: `src/schemas.py`
- `class Category(str, Enum)` — 13 members: `BASIC_DETAILS`, `PERSONAL_DETAILS`, `AMAR_TAKHSEES`, `KEY_HANDOVER`, `CONTRACT`, `EWA_LETTERS`, `RENT_DEDUCTION`, `ALLOWANCE_DEDUCTION`, `NOTIFICATIONS`, `MAINTENANCE`, `PICTURES`, `MODIFICATIONS`, `OTHER_LETTERS`
- `class PageClassification(BaseModel)` — fields: `house_number: str`, `resident: str`, `category: Category`, `is_continuation: bool`

### Refactored file: `src/llm.py`
- `class InvalidResponseError(Exception)` — new exception for schema validation failures
- `class RateLimitError(Exception)` — retained
- `class GemmaClient.__init__(self, api_keys, delay_between_pages)` — adds `delay_between_pages: float = 1.0`
- `GemmaClient._build_system_prompt() -> str` — returns the classification system prompt with 13 categories + Arabic/English descriptions + special rules
- `GemmaClient.classify_page(self, image_bytes: bytes, previous_summary: str) -> PageClassification` — replaces `process_image()`, sends image via `types.Part.from_bytes()` for multimodal vision
- Retry decorator: `@retry(wait=wait_exponential(multiplier=2, min=4, max=60) + wait_random(0, 2), stop=stop_after_attempt(7), retry=retry_if_exception_type((RateLimitError, InvalidResponseError)))`
- `temperature=0` in `GenerateContentConfig`

### Modified file: `requirements.txt`
- Added line: `pydantic`

---

## Tasks

<task id="02-01-01">
<objective>Create the `src/schemas.py` file with `Category` enum and `PageClassification` Pydantic model</objective>

<read_first>
- .planning/phases/02-llm-integration/02-RESEARCH.md (lines 28–47: schema definition)
- .planning/phases/02-llm-integration/02-CONTEXT.md (D-08: NONE sentinel)
- .planning/PROJECT.md (line 25: 13 category names)
</read_first>

<action>
Create `src/schemas.py` with:

1. Import `Enum` from `enum`, `BaseModel`, `Field`, and `field_validator` from `pydantic`.

2. Define `class Category(str, Enum)` with exactly 13 members:
   - BASIC_DETAILS = "basic_details"
   - PERSONAL_DETAILS = "personal_details"
   - AMAR_TAKHSEES = "amar_takhsees"
   - KEY_HANDOVER = "key_handover_form"
   - CONTRACT = "contract"
   - EWA_LETTERS = "ewa_related_letters"
   - RENT_DEDUCTION = "rent_deduction"
   - ALLOWANCE_DEDUCTION = "allowance_deduction"
   - NOTIFICATIONS = "notifications"
   - MAINTENANCE = "maintenance"
   - PICTURES = "pictures"
   - MODIFICATIONS = "modifications"
   - OTHER_LETTERS = "other_letters"

3. Define `class PageClassification(BaseModel)` with fields:
   - house_number: str = Field(description="The house number mentioned in the document")
   - resident: str = Field(description="The resident name in Arabic, or 'NONE' for general house letters and Amar Takhsees")
   - category: Category = Field(description="The document category from the 13 defined types")
   - is_continuation: bool = Field(description="True if this page continues the same topic as the previous page")

   Add a field validator for `resident` using `@field_validator('resident', mode='before')` that strips whitespace and converts the string to uppercase so " none " becomes "NONE".

4. Add `pydantic` to `requirements.txt` (new line after existing entries).
</action>

<acceptance_criteria>
- `src/schemas.py` exists and is importable: `python -c "from src.schemas import Category, PageClassification"`
- `Category` enum has exactly 13 members: `python -c "from src.schemas import Category; assert len(Category) == 13"`
- All 13 enum values match: `python -c "from src.schemas import Category; assert Category.BASIC_DETAILS.value == 'basic_details'; assert Category.OTHER_LETTERS.value == 'other_letters'"`
- `PageClassification` has exactly 4 fields named `house_number`, `resident`, `category`, `is_continuation`: `python -c "from src.schemas import PageClassification; assert set(PageClassification.model_fields.keys()) == {'house_number', 'resident', 'category', 'is_continuation'}"`
- `PageClassification(house_number='683', resident=' none ', category='amar_takhsees', is_continuation=False)` validates successfully and `.resident` equals `'NONE'`
- `requirements.txt` contains the line `pydantic`
</acceptance_criteria>

<verify>
python -c "from src.schemas import Category, PageClassification; assert len(Category) == 13; p = PageClassification(house_number='683', resident=' none ', category='amar_takhsees', is_continuation=False); assert p.resident == 'NONE'; assert p.category == Category.AMAR_TAKHSEES; print('PASS')"
</verify>
</task>

<task id="02-01-02">
<objective>Refactor `GemmaClient` in `src/llm.py` to use image-based multimodal input, `gemma-4-31b-it` model, native JSON schema, and improved retry logic</objective>

<read_first>
- src/llm.py (current state — 74 lines, process_image method)
- src/schemas.py (just created — Category, PageClassification)
- requirements.txt (verify tenacity is installed)
- .planning/phases/02-llm-integration/02-RESEARCH.md (lines 49–62: API call pattern; lines 201–218: retry config)
- .planning/phases/02-llm-integration/02-CONTEXT.md (D-01, D-02, D-05, D-06, D-07, D-08)
</read_first>

<action>
Refactor `src/llm.py`:

1. Add imports: `from src.schemas import PageClassification`, `import time`, `import json`.

2. Add new exception class `InvalidResponseError(Exception)` alongside existing `RateLimitError`.

3. Update `GemmaClient.__init__` signature to `(self, api_keys: list[str] = None, delay_between_pages: float = 1.0)`. Store `self.delay_between_pages = delay_between_pages`.

4. Add method `_build_system_prompt(self) -> str` that returns the full classification system prompt containing:
   - Role: "Arabic document classification expert analyzing scanned housing files from Bahrain/Gulf region"
   - All 13 categories with bilingual descriptions (English identifier + Arabic name)
   - Special rules for NONE resident (D-08), name normalization (D-06), OCR noise tolerance
   - Instruction that the model is receiving a scanned page image and should read it directly

5. Remove `process_image()` method entirely.

6. Add new method `classify_page(self, image_bytes: bytes, previous_summary: str = "") -> PageClassification`:
   - Builds the user prompt including `previous_summary` for continuation context
   - Calls `self.client.models.generate_content()` with:
     - model="gemma-4-31b-it"
     - contents=[system_prompt, types.Part.from_bytes(data=image_bytes, mime_type='image/png')]
     - config=types.GenerateContentConfig(response_mime_type="application/json", response_schema=PageClassification, thinking_config=types.ThinkingConfig(thinking_budget=0), temperature=0)
   - Returns `response.parsed` if not None
   - Falls back to `json.loads(response.text)` validated through `PageClassification(**data)` if `response.parsed` is None
   - Raises `InvalidResponseError` if both fail
   - On 429/rate-limit exceptions: calls `self.switch_key()` then raises `RateLimitError`
   - Calls `time.sleep(self.delay_between_pages)` after a successful call

7. Apply retry decorator to `classify_page`:
   `@retry(wait=wait_exponential(multiplier=2, min=4, max=60) + wait_random(0, 2), stop=stop_after_attempt(7), retry=retry_if_exception_type((RateLimitError, InvalidResponseError)))`
</action>

<acceptance_criteria>
- `src/llm.py` contains no reference to `process_image` or `gemini-2.5-flash`
- `src/llm.py` contains the string `gemma-4-31b-it` exactly once
- `src/llm.py` imports `PageClassification` from `src.schemas`
- `GemmaClient` has method `classify_page` with parameters `(self, image_bytes: bytes, previous_summary: str = "")`
- `GemmaClient` has method `_build_system_prompt` that returns a string containing all 13 category identifiers
- The `classify_page` method sends the image via `types.Part.from_bytes(data=image_bytes, mime_type='image/png')`
- The retry decorator uses `multiplier=2`, `min=4`, `max=60`, `stop_after_attempt(7)`
- `InvalidResponseError` class is defined in the module
- `GenerateContentConfig` includes `response_mime_type="application/json"`, `response_schema=PageClassification`, `thinking_config=types.ThinkingConfig(thinking_budget=0)`, `temperature=0`
- `self.delay_between_pages` is stored and used via `time.sleep()`
</acceptance_criteria>

<verify>
python -c "import ast; tree = ast.parse(open('src/llm.py').read()); classes = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]; assert 'GemmaClient' in classes; assert 'InvalidResponseError' in classes; methods = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'classify_page' in methods; assert '_build_system_prompt' in methods; assert 'process_image' not in methods; text = open('src/llm.py').read(); assert 'gemma-4-31b-it' in text; assert 'gemini-2.5-flash' not in text; assert 'response_mime_type' in text; assert 'ThinkingConfig' in text; assert 'Part.from_bytes' in text; assert 'image/png' in text; print('PASS')"
</verify>
</task>
