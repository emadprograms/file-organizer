---
title: "Test Infrastructure — Wave 0 Stubs & Fixtures"
phase: 02
wave: 1
depends_on: []
files_modified:
  - tests/test_llm.py
  - tests/conftest.py
  - tests/__init__.py
  - requirements.txt
requirements:
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04
  - LLM-05
autonomous: true
---

# Plan 02 — Test Infrastructure

## Goal

Create the test stubs and shared fixtures referenced in `02-VALIDATION.md` Wave 0. These stubs will initially be skeleton tests that import the schemas and client, validate basic schema behavior, and prepare mocked API fixtures for later integration tests.

## must_haves

- [ ] `tests/conftest.py` exists with shared fixtures for mocked API responses
- [ ] `tests/test_llm.py` exists with test stubs for all 6 verification map entries
- [ ] Schema unit tests pass without any API call
- [ ] `pytest` added to `requirements.txt`

## Artifacts this phase produces

### New file: `tests/__init__.py`
- Empty init file to make `tests` a package

### New file: `tests/conftest.py`
- `fixture mock_api_response()` — returns a mock `PageClassification` instance with house_number="683", resident="محمد", category=Category.BASIC_DETAILS, is_continuation=False
- `fixture mock_continuation_response()` — returns a `PageClassification` with is_continuation=True
- `fixture mock_none_resident_response()` — returns a `PageClassification` with resident="NONE", category=Category.AMAR_TAKHSEES
- `fixture sample_arabic_text()` — returns a string of sample Arabic document text for testing prompts
- `fixture mock_gemma_client(monkeypatch)` — patches `GemmaClient.classify_page` to return the `mock_api_response` fixture

### New file: `tests/test_llm.py`
- `test_api_connection()` — verifies `GemmaClient` can be instantiated with a dummy key without crashing
- `test_schema_definition()` — verifies `Category` has 13 members and `PageClassification` validates correctly
- `test_page_classification()` — stub for integration test (marked `pytest.mark.skip` until Plan 01 is done)
- `test_category_classification()` — verifies all 13 `Category` enum values are accepted by `PageClassification`
- `test_continuation_detection()` — stub (marked `pytest.mark.skip`)
- `test_sliding_window()` — stub (marked `pytest.mark.skip`)

### Modified file: `requirements.txt`
- Added line: `pytest`

---

## Tasks

<task id="02-02-01">
<objective>Create `tests/__init__.py`, `tests/conftest.py` with shared fixtures for mocked LLM responses</objective>

<read_first>
- .planning/phases/02-llm-integration/02-VALIDATION.md (Wave 0 requirements, lines 52–57)
- .planning/phases/02-llm-integration/02-RESEARCH.md (lines 28–47: schema fields and values)
- src/schemas.py (Category enum values, PageClassification fields — created by Plan 01)
</read_first>

<action>
1. Create empty `tests/__init__.py`.

2. Create `tests/conftest.py` with:
   - Import `pytest`, `from unittest.mock import MagicMock, patch`
   - Import `from src.schemas import Category, PageClassification`
   - Import `from src.llm import GemmaClient`

   - `@pytest.fixture` def `mock_api_response()`: returns `PageClassification(house_number="683", resident="محمد", category=Category.BASIC_DETAILS, is_continuation=False)`
   - `@pytest.fixture` def `mock_continuation_response()`: returns `PageClassification(house_number="683", resident="محمد", category=Category.BASIC_DETAILS, is_continuation=True)`
   - `@pytest.fixture` def `mock_none_resident_response()`: returns `PageClassification(house_number="683", resident="NONE", category=Category.AMAR_TAKHSEES, is_continuation=False)`
   - `@pytest.fixture` def `sample_arabic_text()`: returns string `"بسم الله الرحمن الرحيم\nرقم البيت: 683\nالاسم: محمد أحمد\nالموضوع: عقد إيجار"`
   - `@pytest.fixture` def `mock_gemma_client(monkeypatch, mock_api_response)`: creates a `GemmaClient` with `api_keys=["test-key"]`, monkeypatches `classify_page` to return `mock_api_response`, returns the client

3. Add `pytest` to `requirements.txt` on a new line.
</action>

<acceptance_criteria>
- `tests/__init__.py` exists (can be empty)
- `tests/conftest.py` exists and is importable
- `tests/conftest.py` contains exactly 5 fixture functions: `mock_api_response`, `mock_continuation_response`, `mock_none_resident_response`, `sample_arabic_text`, `mock_gemma_client`
- `requirements.txt` contains the line `pytest`
- Running `python -c "from tests.conftest import *"` does not raise ImportError (assuming schemas exist)
</acceptance_criteria>

<verify>
python -c "import ast; tree = ast.parse(open('tests/conftest.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'mock_api_response' in funcs; assert 'mock_continuation_response' in funcs; assert 'mock_none_resident_response' in funcs; assert 'sample_arabic_text' in funcs; assert 'mock_gemma_client' in funcs; print('PASS')"
</verify>
</task>

<task id="02-02-02">
<objective>Create `tests/test_llm.py` with test stubs matching the verification map from 02-VALIDATION.md</objective>

<read_first>
- tests/conftest.py (just created — fixture names and return types)
- .planning/phases/02-llm-integration/02-VALIDATION.md (lines 39–47: verification map with task IDs and test names)
- src/schemas.py (Category, PageClassification — for schema validation tests)
- src/llm.py (GemmaClient — for instantiation test)
</read_first>

<action>
Create `tests/test_llm.py` with:

1. Imports: `pytest`, `from src.schemas import Category, PageClassification`, `from src.llm import GemmaClient`

2. `def test_api_connection()`:
   - Instantiates `GemmaClient(api_keys=["test-key-1234"])` 
   - Asserts `client.api_keys == ["test-key-1234"]`
   - Asserts `client.current_key_idx == 0`
   - Asserts `hasattr(client, 'classify_page')`

3. `def test_schema_definition()`:
   - Asserts `len(Category) == 13`
   - Creates `PageClassification(house_number="683", resident="محمد", category=Category.BASIC_DETAILS, is_continuation=False)`
   - Asserts each field value matches
   - Creates another with `resident="NONE"`, `category=Category.AMAR_TAKHSEES` — asserts it validates

4. `def test_page_classification(mock_gemma_client, sample_arabic_text)`:
   - Calls `mock_gemma_client.classify_page(page_text=sample_arabic_text)`
   - Asserts result is a `PageClassification` instance
   - Asserts `result.house_number == "683"`

5. `def test_category_classification()`:
   - Iterates over all 13 `Category` members
   - For each, creates a `PageClassification(house_number="1", resident="test", category=cat, is_continuation=False)` and asserts `.category == cat`

6. `@pytest.mark.skip(reason="Requires sliding window from Plan 03")` 
   `def test_continuation_detection()`:
   - pass (stub)

7. `@pytest.mark.skip(reason="Requires sliding window from Plan 03")`
   `def test_sliding_window()`:
   - pass (stub)
</action>

<acceptance_criteria>
- `tests/test_llm.py` contains exactly 6 test functions: `test_api_connection`, `test_schema_definition`, `test_page_classification`, `test_category_classification`, `test_continuation_detection`, `test_sliding_window`
- `test_continuation_detection` and `test_sliding_window` are decorated with `@pytest.mark.skip`
- Running `python -m pytest tests/test_llm.py -x -q` produces: 4 passed, 2 skipped (assuming Plan 01 tasks are also complete)
- No test function makes a real API call to Google
</acceptance_criteria>

<verify>
python -m pytest tests/test_llm.py -x -q
</verify>
</task>
