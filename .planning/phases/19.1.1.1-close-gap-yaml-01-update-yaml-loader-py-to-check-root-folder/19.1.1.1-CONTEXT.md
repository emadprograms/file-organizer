# Phase 19.1.1.1: close-gap-yaml-01-update-yaml-loader-py-to-check-root-folder - Context

**Gathered:** 2026-07-17
**Status:** Ready for planning

<domain>
## Phase Boundary

Update `yaml_loader.py` to check the `.source_files/` directory for the YAML configuration instead of the root `target_dir`.

</domain>

<decisions>
## Implementation Decisions

### YAML Filename and Location
- **D-01:** The pipeline must look for exactly `target_dir / .source_files / {house_id}_tenants.yaml`.
- **D-02:** The `.source_files/` folder will be located inside the `target_dir` (the folder being organized). The filename will remain `{house_id}_tenants.yaml`.
- **D-03:** Since we are targeting a specific `{house_id}_tenants.yaml` filename, any other `.yaml` files in the `.source_files/` directory should be safely ignored.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Source Code
- `src/tenant_config/yaml_loader.py` — The YAML configuration loading logic to be updated.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `yaml_loader.py` logic: Can be adapted directly without creating a new file.

### Established Patterns
- Path manipulation: using `pathlib.Path` with `/` operator.

### Integration Points
- `src/tenant_config/yaml_loader.py`: `load_tenant_config` function.

</code_context>

<specifics>
## Specific Ideas

The user explicitly confirmed: "the pipeline should look for yaml inside the 1273/.source_files/1273_tenants.yaml, that is the correct way."

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 19.1.1.1-close-gap-yaml-01-update-yaml-loader-py-to-check-root-folder*
*Context gathered: 2026-07-17*
