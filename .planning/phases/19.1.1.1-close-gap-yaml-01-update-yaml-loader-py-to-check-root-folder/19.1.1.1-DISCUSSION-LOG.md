# Phase 19.1.1.1: close-gap-yaml-01-update-yaml-loader-py-to-check-root-folder - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-17
**Phase:** 19.1.1.1-close-gap-yaml-01-update-yaml-loader-py-to-check-root-folder
**Areas discussed:** YAML Filename and Location

---

## YAML Filename and Location

| Option | Description | Selected |
|--------|-------------|----------|
| Look for YAML inside house folder | e.g. `1273 house file/.source_files/1273_tenants.yaml` | ✓ |
| Look for YAML directly in house folder | e.g. `1273 house file/1273_tenants.yaml` | |
| Look for YAML in parent folder under .source_files | e.g. `parent_folder/.source_files/1273_tenants.yaml` | |
| Look for YAML directly in parent folder | e.g. `parent_folder/1273_tenants.yaml` | |

**User's choice:** Look for YAML inside house folder under .source_files
**Notes:** User originally placed everything inside the parent PDFs folder and expected `.source_files` there, but realized this was causing confusion. They confirmed that the YAML should correctly be found in `{house_id}/.source_files/{house_id}_tenants.yaml`.

---

## the agent's Discretion

None

## Deferred Ideas

None
