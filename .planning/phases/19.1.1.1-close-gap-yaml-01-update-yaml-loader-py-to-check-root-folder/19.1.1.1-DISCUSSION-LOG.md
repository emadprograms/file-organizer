# Phase 19.1.1.1 Discussion Log

## Topic: YAML Filename and Location
**Options Presented:**
1. Yes, it will be `target_dir / .source_files / {house_id}_tenants.yaml`
2. It will be `target_dir / .source_files / tenants.yaml` (static filename)
3. It will just be the only `.yaml` file inside `target_dir / .source_files/`

**User Selection:** Yes, it will be `target_dir / .source_files / {house_id}_tenants.yaml`
**Notes:** The YAML file will remain named based on the house ID, but placed inside a `.source_files` directory inside the target directory.
