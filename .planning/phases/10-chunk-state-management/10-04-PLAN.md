---
phase: 10-chunk-state-management
plan: 04
type: execute
wave: 3
depends_on: [10-02]
files_modified: [tests/test_grouping.py, .planning/phases/10-chunk-state-management/VALIDATION.md]
autonomous: true
requirements: [GRP-01, GRP-02, GRP-03, GRP-04]
must_haves:
  truths:
    - Every requirement GRP-01 through GRP-04 is mapped to a passing test.
    - The system survives a simulated "Hard Halt" and resumes perfectly.
    - The [5, 3, 2] shrink sequence is verified end-to-end.
  artifacts:
    - .planning/phases/10-chunk-state-management/VALIDATION.md
  key_links:
    - VALIDATION.md -> Test Suite
---

<objective>
Perform final verification of the Chunk State Management phase, ensuring all requirements are met and the system is resilient to hard halts.

Purpose: To provide a "ground truth" for phase success and verify the integration of state, shrink, and merge logic.
Output: A completed VALIDATION.md file and a passing resilience test suite.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-chunk-state-management/10-CONTEXT.md
@.planning/phases/10-chunk-state-management/10-RESEARCH.md
@tests/test_grouping.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VALIDATION.md Mapping</name>
  <files>.planning/phases/10-chunk-state-management/VALIDATION.md</files>
  <action>
    Create the `VALIDATION.md` file. 
    Map each requirement (GRP-01: Dynamic Chunking, GRP-02: State Reset, GRP-03: Graceful Halt, GRP-04: Anchor Merge) to the specific test cases implemented in `tests/test_grouping.py`.
    Include a section for "Resilience Verification" covering hard-halt recovery.
  </action>
  <verify>
    <automated>ls .planning/phases/10-chunk-state-management/VALIDATION.md</automated>
  </verify>
  <done>VALIDATION.md exists and correctly maps requirements to tests.</done>
</task>

<task type="auto">
  <name>Task 2: Final Resilience Integration Audit</name>
  <files>tests/test_grouping.py</files>
  <action>
    Execute the full suite of grouping tests.
    Specifically verify the "Hard Halt" scenario: 
    1. Run grouping.
    2. Force exit during process.
    3. Restart and verify `current_page_index` is recovered from `grouping_state.json`.
    4. Verify that the shrink sequence 5->3->2 works across the restart if failure count was persisted.
  </action>
  <verify>
    <automated>pytest tests/test_grouping.py</automated>
  </verify>
  <done>All grouping tests pass and resilience is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test Environment -> Production | Ensure mocks don't hide real-world failure modes |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Reliability | Integration | low | mitigate | Comprehensive E2E tests simulating multiple halt/resume cycles. |
</threat_model>

<verification>
Check that `VALIDATION.md` is updated to "Pass" for all items after the final test run.
</verification>

<success_criteria>
All requirements in VALIDATION.md are mapped to passing tests.
</success_criteria>

<output>
Create `.planning/phases/10-chunk-state-management/10-04-SUMMARY.md` when done
</output>
