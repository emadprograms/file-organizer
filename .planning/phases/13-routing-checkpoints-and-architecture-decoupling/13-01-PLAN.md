---
phase: 13-routing-checkpoints-and-architecture-decoupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/core/config.py, src/processing/routing/state.py]
autonomous: true
requirements: [RES-01, CFG-01]

must_haves:
  truths:
    - Routing state stores the actual folder path for each processed index.
    - A default routing model is defined in the global configuration.
  artifacts:
    - src/core/config.py updated with ROUTING_MODEL.
    - src/processing/routing/state.py updated to use results dictionary.
  key_links:
    - RoutingStateManager must be able to serialize/deserialize the new results dict.
---

<objective>
Refactor the routing state schema and configuration to support result persistence and explicit model selection.

Purpose: Fix the root cause of routing data loss (tracking indices instead of results) and establish a configuration point for the routing model.
Output: Updated state schema and global config.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/config.py
@src/processing/routing/state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ROUTING_MODEL to global config</name>
  <files>src/core/config.py</files>
  <action>Add a `ROUTING_MODEL` constant to `src/core/config.py`. It should use `os.getenv("ROUTING_MODEL", "google/gemma-4-26b-a4b-it")` to allow environment-based overrides while providing a sensible default.</action>
  <verify>
    <automated>grep "ROUTING_MODEL" src/core/config.py</automated>
  </verify>
  <done>ROUTING_MODEL is defined in config.py</done>
</task>

<task type="auto">
  <name>Task 2: Refactor RoutingState to store results</name>
  <files>src/processing/routing/state.py</files>
  <action>
    In `RoutingState` (Pydantic model):
    1. Remove `processed_indices: List[int]`.
    2. Add `results: dict[int, str] = Field(default_factory=dict, description="Mapping of document index to assigned folder path.")`.
    3. Add `grouping_checksum: str | None = None` to persist the checksum active when routing results were computed (per D-04).
    4. Update the class docstring to reflect that it now stores actual routing results and the grouping checksum.
  </action>
  <verify>
    <automated>grep "results: dict\[int, str\]" src/processing/routing/state.py</automated>
  </verify>
  <done>RoutingState uses a results dictionary and tracks the grouping checksum</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Disk -> Memory | Routing state JSON is loaded into Pydantic model |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | _routing.json | low | mitigate | Pydantic validation of the `results` dict types during `load_state`. |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages added in this plan. |
</threat_model>

<verification>
Verify that a `RoutingState` object can be initialized, updated with a result, and serialized/deserialized without losing the mapping.
</verification>

<success_criteria>
- `ROUTING_MODEL` exists in config.py.
- `RoutingState` uses `results: dict[int, str]`.
- Pydantic validation is maintained.
</success_criteria>

<output>
Create `.planning/phases/13-routing-checkpoints-and-architecture-decoupling/13-01-SUMMARY.md` when done
</output>
