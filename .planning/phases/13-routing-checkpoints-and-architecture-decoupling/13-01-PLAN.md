---
phase: 13-routing-checkpoints-and-architecture-decoupling
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [src/processing/routing/state.py, src/processing/routing/router.py, tests/test_routing.py]
autonomous: true
requirements: [RES-01, CFG-01]
must_haves:
  truths:
    - Routing state is persisted to a JSON file.
    - `route_document` and `double_check_others` accept a dynamic `model` parameter.
  artifacts:
    - src/processing/routing/state.py
  key_links:
    - RoutingStateManager -> Disk (Atomic JSON)
---

<objective>
Implement the `RoutingStateManager` for atomic state persistence and update routing functions to accept a dynamic LLM model parameter.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-routing-checkpoints-and-architecture-decoupling/13-CONTEXT.md
</context>

<feature>
  <name>Routing State and Dynamic Model</name>
  <files>src/processing/routing/state.py, src/processing/routing/router.py, tests/test_routing.py</files>
  <behavior>
    - `RoutingState` schema tracks `processed_indices` (list of integers) and `grouping_checksum` (string) for sanity checking.
    - `RoutingStateManager` atomically saves and loads state to disk, maintaining a `.bak` file, mirroring `GroupingStateManager`.
    - `route_document` and `double_check_others` in `router.py` accept an optional `model` string parameter that defaults to None. When calling `llm_client.generate_content`, pass the `model` parameter.
  </behavior>
  <implementation>
    1. Create `src/processing/routing/state.py` with `RoutingState` and `RoutingStateManager` using Pydantic and `os.replace` (re-use logic/blueprint from `GroupingStateManager`).
    2. Update `double_check_others` and `route_document` in `src/processing/routing/router.py` to accept `model: Optional[str] = None`.
    3. Pass the `model` parameter down to `llm_client.generate_content` in both functions.
    4. Write tests in `tests/test_routing.py` for state persistence and ensuring the model parameter is passed.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Routing State Manager and Model Config</name>
  <files>src/processing/routing/state.py, src/processing/routing/router.py, tests/test_routing.py</files>
  <behavior>
    - State is saved and loaded atomically.
    - `model` parameter is correctly propagated.
  </behavior>
  <action>
    Create `src/processing/routing/state.py`. Add tests in `tests/test_routing.py`. Update `router.py` to accept and pass the `model` parameter.
  </action>
  <verify>
    <automated>pytest tests/test_routing.py</automated>
  </verify>
  <done>RoutingStateManager is implemented and routing functions accept model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Disk -> StateManager | Untrusted/Corrupted JSON file read from disk |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | State File | medium | mitigate | Use Pydantic schema validation on load; ignore/reset if invalid. |
| T-13-02 | Denial of Service | State File | low | mitigate | Atomic write via temp file prevents partial write corruption. |
</threat_model>

<success_criteria>
pytest tests/test_routing.py passes.
</success_criteria>

<output>
Create `.planning/phases/13-routing-checkpoints-and-architecture-decoupling/13-01-SUMMARY.md` when done
</output>
