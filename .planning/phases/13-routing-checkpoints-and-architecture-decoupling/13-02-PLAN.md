---
phase: 13-routing-checkpoints-and-architecture-decoupling
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: [src/processing/pipeline.py]
autonomous: true
requirements: [ARCH-01, RES-01, CFG-01]

must_haves:
  truths:
    - Pipeline decouples grouping from routing into separate methods.
    - Resuming routing restores folder paths from the results dictionary.
    - Routing functions receive the routing_model explicitly.
  artifacts:
    - src/processing/pipeline.py updated with _group_documents and _route_documents.
  key_links:
    - Pipeline._route_documents correctly interfaces with RoutingState.results.
---

<objective>
Decouple the pipeline architecture and fix the routing resumption bug by restoring state into document objects.

Purpose: Eliminate the "Skip-and-Forget" bug where routed documents lost their folder paths upon resumption, and improve modularity.
Output: Refactored Pipeline class.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/processing/pipeline.py
@src/core/config.py
@src/processing/routing/state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Pipeline __init__ for routing model</name>
  <files>src/processing/pipeline.py</files>
  <action>
    Update `Pipeline.__init__` to accept an optional `routing_model: str = None`. 
    Store it as `self.routing_model`. If `None` is passed, default it to `src.core.config.ROUTING_MODEL`.
  </action>
  <verify>
    <automated>grep "self.routing_model" src/processing/pipeline.py</automated>
  </verify>
  <done>Pipeline explicitly tracks the routing model</done>
</task>

<task type="auto">
  <name>Task 2: Decouple grouping and routing methods</name>
  <files>src/processing/pipeline.py</files>
  <action>
    1. Extract the grouping logic from `_group_and_route_documents` into a new private method `_group_documents(self, raw_pages, run_checkpoint_path)`. This method should return `list[DocumentGroup]`.
    2. Extract the routing logic (from the checksum calculation onwards) into a new private method `_route_documents(self, documents, run_checkpoint_path)`. This method should modify the documents in place and return them.
    3. Refactor `_group_and_route_documents` to simply call `_group_documents` followed by `_route_documents`.
  </action>
  <verify>
    <automated>grep -E "_group_documents|_route_documents" src/processing/pipeline.py</automated>
  </verify>
  <done>Pipeline logic is decoupled into functional stages</done>
</task>

<task type="auto">
  <name>Task 3: Fix routing loop for resumption and model propagation</name>
  <files>src/processing/pipeline.py</files>
  <action>
    In `_route_documents`:
    1. PRE-ROUTE SANITY CHECK (per D-04): Before entering the routing loop, compute the current grouping checksum. Compare it with `state.grouping_checksum`. If they mismatch and `state.results` is not empty, reset `state.results = {}` to avoid incorrect assignments based on outdated grouping.
    2. Update the routing loop to use `state.results` instead of `state.processed_indices`.
    3. IMPORTANT: Before `continue`, restore the result: `if i in state.results: doc.folder_path = state.results[i]; continue`.
    4. Pass the explicit model to the router: `route_document(doc, self.client, model=self.routing_model)`.
    5. Update state persistence: set `state.results[i] = folder`, store the current grouping checksum in `state.grouping_checksum`, then explicitly call `routing_state_manager.save_state(state)` to persist the result to disk per D-01.
  </action>
  <verify>
    <automated>grep "doc.folder_path = state.results\[i\]" src/processing/pipeline.py</automated>
  </verify>
  <done>Routing resumption is fixed, sanity check is implemented (D-04), and model is explicitly propagated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Pipeline -> Router | Passing model string to LLM client |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02 | Denial of Service | Routing Loop | low | mitigate | Use existing checksum check to avoid infinite routing loops on inconsistent state. |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages added in this plan. |
</threat_model>

<verification>
Verify that the routing logic no longer skips indices without restoring their folder paths.
</verification>

<success_criteria>
- `_group_documents` and `_route_documents` are distinct methods.
- `_route_documents` restores `folder_path` from `state.results`.
- `route_document` is called with `model=self.routing_model`.
</success_criteria>

<output>
Create `.planning/phases/13-routing-checkpoints-and-architecture-decoupling/13-02-SUMMARY.md` when done
</output>
