---
phase: 13-routing-checkpoints-and-architecture-decoupling
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified: [tests/test_pipeline_routing.py]
autonomous: true
requirements: [RES-01]

must_haves:
  truths:
    - Resuming routing restores all previously assigned folder paths.
    - The designated routing model is actually used by the LLM client.
    - Modifying grouping results between interruptions and resumption triggers a full routing reset (D-04).
  artifacts:
    - tests/test_pipeline_routing.py containing resilience and sanity tests.
  key_links:
    - Pipeline resumption test verifies the full cycle: Route -> Interrupt -> Resume -> Verify.
    - Sanity check test verifies: Route -> Interrupt -> Modify Grouping -> Resume -> Reset.
---

<objective>
Implement rigorous E2E verification for routing resilience, model propagation, and state sanity.

Purpose: Prove that the "Skip-and-Forget" bug is eliminated, that the routing model is correctly propagated, and that stale grouping state is detected.
Output: Comprehensive test suite for routing.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/file-organizer/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/processing/pipeline.py
@src/processing/routing/state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Routing Resumption E2E Test</name>
  <files>tests/test_pipeline_routing.py</files>
  <behavior>
    - Setup: Create a set of mocked DocumentGroups.
    - Action 1: Run routing for a subset of documents.
    - Action 2: Save the `_routing.json` state to disk.
    - Action 3: Initialize a new Pipeline and resume routing.
    - Expected: All documents (both previously routed and new) have a valid `folder_path`. No data loss for the first subset.
  </behavior>
  <action>Create `tests/test_pipeline_routing.py` and implement `test_resumption_persistence`. Use temporary directories for checkpoints.</action>
  <verify>
    <automated>pytest tests/test_pipeline_routing.py::test_resumption_persistence</automated>
  </verify>
  <done>Resumption persistence is verified automatically</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify Explicit Model Propagation</name>
  <files>tests/test_pipeline_routing.py</files>
  <behavior>
    - Setup: Initialize Pipeline with a specific `routing_model="test-model-123"`.
    - Action: Trigger the routing process for one document.
    - Expected: The `LLMClient.generate_content` call is made with `model="test-model-123"`.
  </behavior>
  <action>Add `test_model_parameter_passed` to `tests/test_pipeline_routing.py` using `unittest.mock` to spy on the LLM client calls.</action>
  <verify>
    <automated>pytest tests/test_pipeline_routing.py::test_model_parameter_passed</automated>
  </verify>
  <done>Explicit model propagation is verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify Routing Sanity Check (Grouping Mismatch)</name>
  <files>tests/test_pipeline_routing.py</files>
  <behavior>
    - Setup: Run routing for a subset of documents and establish a checkpoint.
    - Action 1: Modify the input pages (e.g., change a category) so that `process_with_shrink` produces a different set of DocumentGroups.
    - Action 2: Resume routing using the existing checkpoint.
    - Expected: The pipeline detects the checksum mismatch (per D-04), resets the routing state, and re-routes ALL documents from the beginning instead of skipping processed indices.
  </behavior>
  <action>Implement `test_routing_sanity_check_grouping_mismatch` in `tests/test_pipeline_routing.py`. Verify that `route_document` call count equals the total number of groups, regardless of the checkpoint's processed indices.</action>
  <verify>
    <automated>pytest tests/test_pipeline_routing.py::test_routing_sanity_check_grouping_mismatch</automated>
  </verify>
  <done>D-04 sanity check is verified to prevent stale state resumption</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test -> Pipeline | Mocked inputs crossing into pipeline logic |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Information Disclosure | Test Logs | low | accept | Test logs are local and do not contain sensitive API keys. |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages added in this plan. |
</threat_model>

<verification>
Run the full test suite and ensure all routing resilience, configuration, and sanity check tests pass.
</verification>

<success_criteria>
- `tests/test_pipeline_routing.py` exists.
- `test_resumption_persistence` passes.
- `test_model_parameter_passed` passes.
- `test_routing_sanity_check_grouping_mismatch` passes.
</success_criteria>

<output>
Create `.planning/phases/13-routing-checkpoints-and-architecture-decoupling/13-03-SUMMARY.md` when done
</output>

<output>
Create `.planning/phases/13-routing-checkpoints-and-architecture-decoupling/13-03-SUMMARY.md` when done
</output>
