---
milestone: v15.0
audited: "2026-09-13T21:35:00+03:00"
status: passed
scores:
  requirements: 17/17
  phases: 5/5
  integration: 8/8
  flows: 8/8
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt: []
---

# Milestone v15.0 Audit Report: Decoupled .NET Core Architecture & Non-Residing Applicants Archive

## Executive Summary

Milestone v15.0 has achieved **100% completion** across all 5 planned phases (Phases 109–113), 8 cross-phase integration flows, and **17/17 requirements**. The repository `housing-application` is now a fully standalone, idiomatic ASP.NET Core 8.0 web application with zero Python runtime dependencies. Non-residing applicants, unfulfilled allocation orders, and canceled allocations are cleanly supported across the database schema, data access layer, REST API endpoints, and web UI without distorting house vacancy calculations or auto-reallocating general documents.

All test suites pass with 100% success rate:
- **Backend xUnit Suite**: 158 passed, 0 failed, 0 skipped
- **Frontend Vitest Suite**: 293 passed (across 28 test suites), 0 failed
- **Compiler Warnings**: 0
- **Critical Gaps**: 0

---

## 3-Source Requirements Coverage Matrix

| Requirement | Category | Phase | Description | Status | Evidence |
|---|---|---|---|---|---|
| **ARCH-01** | Architecture | Phase 109 | Completely remove all legacy Python source files (`src/` Python files, `.venv/`, `requirements.txt`, `patch_index.py`, and Python pytest files in `tests/`). | satisfied | Verified: 0 python files in `src/` and `tests/`; `.venv`, `requirements.txt`, `patch_index.py` deleted. |
| **ARCH-02** | Architecture | Phase 109 | Reorganize ASP.NET Core 8.0 project from `web-net/` into an idiomatic .NET layout: `src/HousingApplication.Web/` and create `HousingApplication.sln`. | satisfied | Verified: `HousingApplication.sln` links `HousingApplication.Web.csproj` and builds cleanly. |
| **ARCH-03** | Architecture | Phase 109 | Consolidate test suites under `tests/`: Move backend tests to `tests/HousingApplication.Tests/` (linking project references to `HousingApplication.Web.csproj`) and keep frontend tests in `tests/frontend/`. | satisfied | Verified: `HousingApplication.Tests.csproj` references `HousingApplication.Web.csproj`; all tests compile and run. |
| **ARCH-04** | Architecture | Phase 109 | Update all Vitest frontend test imports across `tests/frontend/` to load static assets directly from `src/HousingApplication.Web/wwwroot/js/`, eliminating legacy path dependencies. | satisfied | Verified: All 28 frontend test suites import from `src/HousingApplication.Web/wwwroot/js/`; 0 legacy import paths. |
| **ARCH-05** | Architecture | Phase 109 | Update `run-mac.sh`, `package.json`, and deployment configs to reference the new project paths with zero Python dependencies, verifying 100% test pass rate. | satisfied | Verified: `run-mac.sh` launches ASP.NET Core directly; `npm run test:frontend` and `dotnet test` pass 100%. |
| **DB-01** | Database | Phase 110 | Update SQLite database schema across `DatabaseInitializer.cs` to add `is_resident INTEGER NOT NULL DEFAULT 1` and `notes TEXT` to `tenants` table with automatic idempotent migrations. | satisfied | Verified: `DatabaseInitializer.cs` and `EnsureSchemaAsync` include schema definitions and `ALTER TABLE` migrations. |
| **DB-02** | Database | Phase 110 | Update data access models (`Tenant.cs`, `TenantDto`, `HouseTenantProfileDto`) and repository methods to read, write, and serialize `is_resident` and `notes`. | satisfied | Verified: Models and DTOs serialize `is_resident` and `notes`; verified by `AddTenantAsync_Applicant_SetsIsResidentZeroAndStoresNotes`. |
| **VCN-01** | Vacancy | Phase 110 | Guard house occupancy queries (`CurrentTenant` and `DurationCategory` in `FileOrganizerRepository.cs`) so that only residing tenants (`is_resident = 1`) are considered, ensuring houses with only applicants remain styled as `Vacant` (`grey`). | satisfied | Verified: `GetHouseCardsAsync_WithOnlyNonResidingApplicant_RemainsVacantGrey` and `GetHouseCardsAsync_WithPastResidentAndApplicant_ShowsPastResidentSubtitleAndVacantGrey`. |
| **VCN-02** | Vacancy | Phase 110 | Safeguard document auto-reallocation (`BulkUpdateTenantsAsync`) so date-window matching (Priority 2) and default fallback (Priority 3) only assign documents to residing tenants (`is_resident = 1`). | satisfied | Verified: `residentTenants` filtering in `BulkUpdateTenantsAsync`; verified by `BulkUpdateTenantsAsync_Reallocation_NeverAssignsDocsToNonResidingApplicant`. |
| **REG-01** | Register | Phase 111 | Update House Profile (`house-profile.js`) to segregate the Tenancy Register into two distinct visual sections: Resident Tenants and Applicants & Unfulfilled Allocations. | satisfied | Verified: `house-profile.js` partitions tenants into `.residents-section` and `.applicants-section`; verified by `house_profile.test.js`. |
| **REG-02** | Register | Phase 111 | Implement distinctive card styling for applicants featuring an `📋 متقدم (لم يسكن)` badge, application/order date, document count, and optional notes. | satisfied | Verified: `.applicant-profile-card.tenant-profile-card`, `.applicant-badge`, `.applicant-notes` rendered; verified by `applicant_workflow.test.js`. |
| **REG-03** | Register | Phase 111 | Enable clicking an applicant's card in House Profile to navigate directly to their dedicated category folders in Folders view. | satisfied | Verified: `card.onclick` sets `window.location.hash` to applicant's tenant route; verified by `applicant_workflow.test.js`. |
| **SET-01** | Settings | Phase 112 | Update House Settings modal (`#tenant-modal` in `tenant-manager.js` and `index.html`) to support adding/editing applicants via a Resident / Applicant toggle, automatically disabling/hiding Present and End Date. | satisfied | Verified: `.tenant-type-select` toggles disable states on Present and End Date and sets `is_resident: 0`; verified by `house_settings_modal.test.js`. |
| **ING-01** | Ingestion | Phase 112 | Update Ingest Station (`ingest-station.js`), Batch Move, and Batch Copy modals to clearly group or badge applicant options in tenant dropdowns (`📋 فلان (متقدم - لم يسكن)`). | satisfied | Verified: `categories-view.js` and `ingest-station.js` format dropdown labels with `📋` and `(متقدم - لم يسكن)`; verified by `batch_operations.test.js`. |
| **TIM-01** | Timeline | Phase 112 | Display an applicant badge in Timeline View and Command Palette search results for documents belonging to applicants. | satisfied | Verified: `TimelineItemDto.IsResident` returned from backend; purple badge rendered in `timeline-view.js`; purple card rendered in `command-palette.js`. |
| **VER-01** | Verification | Phase 113 | Implement backend xUnit tests covering the `is_resident` schema migration, vacancy calculation with non-residing applicants, and auto-reallocation guardrails. | satisfied | Verified: 158 xUnit tests pass including 6 repository unit tests and 4 API integration tests. |
| **VER-02** | Verification | Phase 113 | Implement frontend Vitest tests covering the segregated Tenancy Register, applicant card rendering, House Settings modal applicant row interactions, and Ingest dropdown options. | satisfied | Verified: 293 Vitest tests pass across 28 files including 6 integrated tests in `applicant_workflow.test.js`. |

---

## Cross-Phase Integration & E2E Flow Audit

| Flow # | Flow Name | Description | Status | Evidence |
|---|---|---|---|---|
| **Flow 1** | Pure .NET Core Architecture | Verify zero Python source files in application and test suites, `HousingApplication.sln` builds, static assets served from `src/HousingApplication.Web/wwwroot`, test references intact. | **Passed** | 0 Python source files in `src/` and `tests/`; `dotnet test HousingApplication.sln` builds and passes cleanly. |
| **Flow 2** | Database Schema & Models | Verify SQLite migration adds `is_resident` and `notes` to `tenants` table; check models/DTOs serialize both fields. | **Passed** | `DatabaseInitializer.cs` and `EnsureSchemaAsync` contain migration; `Tenant.cs`, `TenantDto`, `HouseTenantProfileDto`, `TreeTenantDto` map fields. |
| **Flow 3** | Vacancy Guardrails | Verify houses with only non-residing applicants remain styled as vacant (`grey`) and do not set an active tenant. | **Passed** | `GetHousesAsync` filters `t.IsResident == 1` for active tenant and subtitle fallback; tested in `RepositoryTests.cs`. |
| **Flow 4** | Document Allocation Guardrails | Verify `BulkUpdateTenantsAsync` reallocation excludes applicants (`is_resident = 0`) from date-window and fallback matching. | **Passed** | Candidate list filtered to `residentTenants = updatedTenants.Where(ut => ut.IsResident == 1).ToList()`; tested in `RepositoryTests.cs`. |
| **Flow 5** | Segregated Tenancy Register | Verify `house-profile.js` partitions tenants into `.residents-section` and `.applicants-section`; verify applicant card styling and folder click route. | **Passed** | Segregated layout with `.applicant-profile-card`, `.applicant-badge`, and route navigation to `#/area/{areaId}/house/{houseId}/tenant/{tenantKey}`; tested in `house_profile.test.js`. |
| **Flow 6** | House Settings Modal | Verify `.tenant-type-select` toggle logic in `tenant-manager.js` and `index.html`. | **Passed** | Responsive 12-column table in `index.html`; `.tenant-type-select` disables Present and End Date and updates Start Date title; tested in `house_settings_modal.test.js`. |
| **Flow 7** | Ingestion & Batch Operations | Verify applicant badging in `ingest-station.js` and `categories-view.js`. | **Passed** | Dropdowns in Ingest Station and Batch Move/Copy modals format applicants with `📋 ${name} (متقدم - لم يسكن)`; tested in `batch_operations.test.js`. |
| **Flow 8** | Timeline & Search | Verify applicant badges in `timeline-view.js` and `command-palette.js`. | **Passed** | Documents mapped with `is_resident`; purple badge rendered in Timeline; purple styling, clipboard icon, and badge rendered in Command Palette; tested in `command_palette_tenants.test.js`. |

---

## Automated Test Suite Summary

- **Backend xUnit Suite**:
  - Command: `export PATH="$HOME/.dotnet:$PATH" && dotnet test HousingApplication.sln`
  - Total: **158**
  - Passed: **158**
  - Failed: **0**
  - Skipped: **0**
  - Duration: 516 ms
- **Frontend Vitest Suite**:
  - Command: `npm run test:frontend`
  - Test Files: **28 passed (28 total)**
  - Total Tests: **293 passed (293 total)**
  - Failed: **0**
  - Skipped: **0**
  - Duration: 3.73s

---

## Tech Debt & Gaps Summary

- **Critical Gaps**: 0
- **Requirements Gaps**: 0
- **Integration Gaps**: 0
- **Flow Gaps**: 0
- **Tech Debt**: None. All legacy Python code purged; architecture clean and self-contained.

---

## Conclusion & Milestone Status

Milestone v15.0 has **PASSED** all criteria:
- **Requirements**: 17/17 satisfied (100%)
- **Phases**: 5/5 verified (100%)
- **Integration Flows**: 8/8 verified (100%)
- **Test Pass Rate**: 100% (158 backend + 293 frontend = 451 total tests passing)

The milestone is fully verified and ready for completion and archiving.
