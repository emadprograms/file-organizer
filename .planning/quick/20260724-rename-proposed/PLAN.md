---
status: in-progress
---

# Quick Task: Rename `_Proposed` to `Proposed`

## Description
The user wants to replace `_Proposed` with `Proposed` everywhere in the codebase.

## Steps
1. Search codebase for `_Proposed`
2. Replace with `Proposed` in all occurrences (source files and tests).
3. Ensure tests pass.
